#+title: Handoff - media-transient backend switching (stage 5)

COMPLETED 2026-08-05 on the Linux box - do not re-run. Stage 5
landed: see the spec's stage-5 section and Progress. Kept for the
record only.

Paste the prompt below into a fresh agent session on either machine
(pure emacs-lab elisp; Linux exercises mpv+mpris, mac mpv+JXA).
Companion to youtube-playback-control.org (the living spec); refresh
the SHAs and re-read the spec's stage 5 before pasting if time has
passed since 2026-08-05.

* Prompt

Continue YouTube playback control from Emacs - stage 5, backend
switching inside the transient.

Read the spec first: ~/.emacs.d/youtube-playback-control.org
(status: stages 1-4 done and pushed, mac and Linux lanes complete;
stage 5 is this task). It is the authoritative living document -
verified findings, design decisions, staged plan. Update Progress
and Changelog as you go. navegosa's own SPEC.md tracks package-level
detail, but stage 5 should not need to touch the package.

Context in one breath: browser-native YT playback is controlled from
Emacs via navegosa (one navegosa-media-* command set; JXA transport
on macOS, MPRIS/dbus on Linux); mpv keeps local files/non-YT; one
media-transient on C-x m with backend-conditional groups
(media-backend: auto/mpv/browser/mpris) shares one key set across
backends; every YT entry point (C-x m o, org yt: links, embark)
routes through media-open. Problem: auto is sticky to mpv while an
mpv process lives - controlling the browser meanwhile means
hand-evaluating a setopt. Stage 5 puts the switch on a key inside
the transient.

Current state (all pushed, trees clean, CI green as of 2026-08-05):
- agzam/navegosa.el @ 40b5060 (V4 MPRIS lane, 109 specs)
- agzam/emacs-lab @ e267a12 plus the stage-5 plan commit carrying
  this file (mpris group + resolver, 923 specs)
First move: fetch and ff-only pull both repos, verify bb lint /
bb test / bb smoke pass untouched before changing anything
(navegosa make test + check-compile too if you end up touching the
package after all).

The work (spec stage 5 has the full detail):
1. A shared suffix on one free key (candidate: b - free in every
   group) in the mpv, browser, AND mpris groups of media-transient
   (modules/web-browsing/autoload/media.el): re-pin media-backend
   for the session among auto / mpv / the platform's browser lane
   (browser on darwin, mpris on gnu/linux - never offer the foreign
   lane). Plain setq, never customize-save.
2. Echo the resolution ("backend: mpris (was auto)") and make the
   transient redraw so the visible group flips. Group :if
   predicates evaluate at setup time - re-invoking
   (transient-setup 'media-transient) from the suffix is the likely
   mechanism; verify live and record the working pattern in the
   spec.
3. Cycle vs prompt is an open micro-decision - pick one, record the
   reason in the spec (cycle: one keypress, three states;
   completing-read: scales, self-documenting).
4. Specs in tests/web-browsing/media-tests.el: switch-function
   state transitions, platform filtering, no persistence side
   effects; binding the key in every group makes the existing
   cross-group key-consistency spec enforce the mirror
   automatically - extend the suffix-set pin with the new command.
5. Live E2E gate: mpv playing a local file AND a browser video
   active at once; hop mpv -> browser lane -> auto from inside
   C-x m, controlling each in turn. On Linux remember: muted tabs
   are invisible to MPRIS - near-silent player volume, never tab
   mute.

Non-negotiable constraints (hard-won, spec's Verified findings):
- Live working environments: the user's browser and Emacs session
  are not test harnesses. Probes non-destructive, own scratch
  players/tabs only, unwind-protect all session state, restore
  everything, elisp-eval hygiene per AGENTS.
- Naming: plain descriptive names - no +prefixes, no invented
  namespaces (AGENTS.md).
- Own packages declare plain GitHub recipes; local-dev-packages +
  local-checkout-recipe (init.el) redirect to ~/GitHub checkouts.
  NEVER commit a literal local :repo - CI smoke dies on it
  (2026-08-05 lesson, one red run on main).
- Verification gates: bb lint, bb test (buttercup output is huge -
  always filter: bb test 2>&1 | rg "FAILED|Ran"), bb smoke; bb
  repair heals stale elpaca builds. Commit only on explicit
  instruction.
