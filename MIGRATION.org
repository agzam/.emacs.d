#+title: Doom migration plan
#+startup: overview

Reshuffle headings to set priority; port top-down. Keep entries updated as
modules land (see AGENTS.md for porting conventions - naming, quarantine,
module layout).

* Ported so far
- core :: elpaca bootstrap (repos/builds in ~/.emacs.d/.local/elpaca/), doom-compat
  (macros, first-hooks incl. switch-frame, modulep!, quarantine), doom-keybinds
  (map!, SPC/"," leaders), doom-defaults (baseline editor/UI defaults,
  doom/escape, MODE-local-vars hooks, built-ins: recentf/saveplace/so-long/
  auto-revert/comint/compile/ediff/show-paren/transient), lazy loaddefs
  generation, XDG cache/state layout
- modules :: evil (lean set), bindings (Doom tree, company/ivy/helm excised,
  SPC p on project.el), git (magit/forge stack, local-dev recipe hook),
  general (incl. smartparens base + persistent-scratch-as-*scratch*),
  completion, embark, colors (circadian stack), modeline (doom-modeline),
  elisp (eval/edebug/profiler + let-plist/evilify-edebug folded in),
  dired (treemacs suite), ai (gptel/eca/mcp), web-browsing,
  tree-sitter, lookup (:tools lookup core, rebuilt plus-free),
  lsp (lsp-mode/lsp-ui/consult-lsp/dap-mode), clojure (cider/clj-refactor/
  neil/cider-storm/monroe), org (core slice + org-cliplink: roam/vulpea/
  evil-org), shell, writing (jinx/translate/dictionaries/prisma),
  search (consult-omni/tldr/slacko),
  tab-bar (workspace transients + desktop persistence, no packages),
  yaml (yaml-mode/jinja2/highlight-indent-guides/yaml-pro),
  pdf (pdf-tools w/ upstream continuous-scroll + saveplace/org-noter/nov)
- user layer :: config.el (leader tree, globals), lisp/functions.el
  incl. reload-config (SPC h r r; doom/reload analogue - re-runs
  defaults/modules/config in place, elpaca picks up new packages; restart
  still needed for doom-compat/doom-keybinds, the elpaca bootstrap and
  active-modules changes; edited autoload/*.el still want a direct
  load-file)
- root packages.el :: ligature, helpful, golden-ratio, lorem-ipsum
  (consult-symbol rode here ahead of search, relocated home 2026-07);
  evil extras stay disabled by omission
- testing/CI :: buttercup suites (tests/, XDG-sandboxed via helper.el),
  bb.edn tasks (deps/lint/test/smoke/ci), scripts/{lint,smoke-check}.el,
  GitHub workflow (test + smoke jobs on Emacs 30.1, cached elpaca dirs)

* Decisions log
- [2026-07] :tools lookup core vendored (new modules/lookup/, loads right
  after bindings) - the decision that gated clojure and unblocks
  golang/lua/consult-dash.  Verdict: rebuild, don't vendor Doom's module
  wholesale.  set-lookup-handlers! + the dispatch engine (lookup--jump-to /
  --run-handlers / --set-handler) + commands + xref/dumb-jump/ffap/
  bug-reference/evil-goto backends ported near-verbatim, plus-free
  (+lookup/definition -> lookup-definition, +lookup-*-functions ->
  lookup-*-functions - so every SPC c / normal g / SPC s row the bindings
  tree already carried resolves).  Deliberate trims: better-jumper ->
  evil-set-jump (records the origin jump); the dictionary/thesaurus
  default backends dropped (+dictionary never enabled in doom.d - they'd be
  void in the default chains, a silent-skip Doom tolerated); the
  ivy/helm online-provider frontends dropped (counsel/helm-only - the
  provider alist keeps plain URL strings, 16 of them); project-search
  backend rides consult-ripgrep (embark-project-search precedent) not
  +vertico-file-search.  doom-compat gained doom-region-{active-p,
  beginning,end} + doom-thing-at-point-or-region (lookup commands' at-point
  grab) and modulep! learned -flag negation (SPC c lsp rows ride
  :tools lsp -eglot).  The stub set-lookup-handlers! left doom-compat -
  an autoload can't override an fboundp stub, so the real one had to move
  to the module loaddefs (the ai-module harness-fn lesson, inverted).
- [2026-07] lsp module ported (lsp-mode, lsp-ui, consult-lsp, dap-mode +
  the useful runtime slice of Doom's :tools lsp +peek; registry slot
  tree-sitter < lsp < clojure, doom!-faithful).  LSP_USE_PLISTS="1" in
  early-init setenv (must be in the env for BOTH the elpaca byte-compile
  and load - lsp-use-plists reads it at load, the compiled lsp-get
  accessors bake it in).  lsp-booster json-parse advice is EAGER
  (modules/lsp/autoload.el) - doom.d registered it from a lazily loaded
  file, leaving a startup window where a booster-wrapped server's bytecode
  output hit an unadvised parser; armed at boot now, no-op without the
  binary.  Kept: deferred server shutdown (lsp-defer-server-shutdown-a),
  the signature-stop-on-escape hook, the lsp-mode-hook localleader map,
  set-lookup-handlers! 'lsp-mode.  That last one MOVED here from
  consult-dash's :config (completion module) - doom.d registered it only
  after the first consult-dash call, so lsp buffers opened earlier got no
  lookup handlers (latent load-order bug).  lsp-ui installed but
  manual-only (lsp-ui-manual-only-a suppresses auto-enable, doom.d ground
  truth: its remove-hook! kept lsp-ui-mode off everywhere).  Dropped as
  doom.d rot (git-resurrectable): lsp-ui-flycheck-list+ and its "ge"/q
  binds - lsp-ui-mode never activates in live doom.d, so
  lsp-ui-flycheck-list--update was void at runtime (bound-but-broken);
  flycheck parked on :checkers anyway.  Doom's +lsp-optimization-mode
  save/restore dance dropped - the root config.el already pins
  read-process-output-max 10MB + gc-cons-threshold 200MB globally.
  Renames: +lsp-completion-at-point -> lsp-completion-at-point-maybe (cape
  combinator-safe), +default/lsp-command-map -> lsp-command-map-dispatch
  (SPC c l - the map is a plain defvar keymap with no function cell, so
  the row needs a dispatcher that loads lsp-mode then set-transient-map's
  it; can't bind the bare map symbol).  lsp!/dap paths + session/server/
  breakpoint files quarantined in doom-compat.el.  consult-lsp
  c j/J rows un-guarded (vertico guard dropped - lab registry is
  :custom completion, org-port precedent).
- [2026-07] clojure module ported (clojure-mode, cider, clj-refactor,
  clojars, neil, logview, datetime, cider-storm, monroe; registry slot
  after lsp).  Entangled with lsp throughout - ported alongside so no
  silent voids: clojure-lookup-handlers-h wires the lsp/cider handlers,
  cape-cider-lsp-yas mixes cider-complete-at-point-maybe +
  lsp-completion-at-point-maybe + yasnippet-capf, lsp-clojure-fqn-at-point
  (lsp-request/lsp-make-* API, dash -some-> chain rewritten with
  when-let*).  Projectile refs -> project.el: the root-file additions
  become project-vc-extra-root-markers, projectile-project-root ->
  doom-project-root.  Renames (plus-ban + package-namespace de-squat,
  collision-checked live): +clojure/* -> bare (clojure-open-repl, ...),
  cider-storm-storm-start-gui+ -> cider-storm-start-gui-styled and
  lsp-clojure-show-project-tree+ -> lsp-clojure-project-tree-toggle (wrap
  package commands), clojure-project-root-path+ ->
  clojure-project-root-path-poly, separedit--*-clj-str-delimeters ->
  clj-str-*-delimiters (typo + squat), spacehammer-monroe--* ->
  hammerspoon-monroe-* (writing-port precedent), +cider-complete-at-point
  -> cider-complete-at-point-maybe.  Kept where cider's namespace is free
  (cider-switch-to-nrepl-buffer, kill-cider-buffers, cider-fqn-symbol-at-
  point, cider-eval-sexp-at-point* keeps its star - plain name is cider's).
  doom.d rot dropped: clojure-enable-fancify-symbols (spacemacs-era free
  var), the duplicate cider-repl-display-in-current-window setq;
  -partial -> apply-partially (dash-free).  spacehammer-monroe glue
  unblocked (writing ported) - installs on darwin only, fennel-mode map!
  waits on the lua module.  expreg-transient Clojure section restored
  ("; c" wrap-rich-comment).  org-cliplink pulled ahead the same sitting
  (warm-up) - package + "i L" localleader + url.el title-retrieval heal.
  Verification: 560 specs (+63: tests/lookup/ x2, tests/lsp/ x2,
  tests/clojure/ x4), smoke 164 -> 191 packages 0 failed/blocked, probe
  green (191 pkgs, all binds resolve incl. SPC c a/l/r/o/S/j/J + c d/D/i/
  k/t/W + evil gd/gD/gf/gI/K, no voids, booster+monroe advice armed at
  boot, LSP_USE_PLISTS=1, quarantine x4, marker-path, no +residue, no new
  *Warnings*).  Keydiff 59->51 open / 59->51 broken / 128 resolved /
  0 regressions / 0 lab-only - SPC c code/lsp closed, SPC k parked
  (sexp-transient), normal-g gd/gD/gI/gf :improved.  Test-infra lesson:
  lookup--run-handlers uses condition-case-unless-debug (Doom's idiom) and
  (get handler 'lookup-async) - buttercup forces debug-on-error (bind it
  nil to test the catch) and handlers must be symbols not lambdas; the
  marker-return path's set-window-configuration hard-aborts buttercup's
  own runner window state, so it lives in the probe not the batch tier.
  elpaca update cycle also closed this sitting (own Decisions entry).
- [2026-07] doom-defaults.el vendored (the settings half of Doom v3's
  lisp/doom-emacs.el @8e4fbba): security/runtime defaults, editor + UI
  defaults, C-i/C-m GUI key translation, doom/escape, MODE-local-vars-hook,
  built-in package config (autorevert, comint, compile, ediff, paren,
  project, recentf, savehist settings, saveplace, so-long, transient).
  Deliberate deviations, in the file header too: winner-mode and
  global-hl-line-mode stay out (custom windows.el engine; hl-line was
  disabled in config.el anyway - its remove-hook there is now inert);
  confirm-kill-emacs is plain yes-or-no-p and kill-current-buffer/
  fallback-buffer advice skipped until a buffers lib ports;
  scroll-conservatively/scroll-margin stay with ultra-scroll; savehist-mode
  enable stays in modules/completion; upstream's saveplace block enables
  savehist-mode (copy-paste bug) - fixed here. doom-compat gained the
  doom-switch-frame-hook trio (debounced, feeds doom-auto-revert) and
  save-place-file quarantine. evil module: evil--doom-escape-a (Doom's
  +evil-escape-a) so ESC in normal state runs doom-escape-hook, plus
  :m [C-i] evil-jump-forward (was referenced in comments, never bound).
- [2026-07] corfu machinery removed entirely: corfu + extensions,
  corfu-terminal, popon, kind-icon, autoload/corfu.el, minad-capf.el.
  Built-in completion-preview-mode + the echo pager (now in
  modules/completion/autoload/completion-preview.el) is the in-buffer UI;
  cape stays (its capfs feed completion-at-point). savehist got its own
  block - its enable used to hide inside corfu-history. Revisit a popup UI
  only if completion-preview falls short.
- [2026-07] projectile dropped config-wide: SPC p rebound to built-in
  project.el (+ consult-project-buffer); consult-projectile and the
  consult-dir projectile source removed. Future ports (e.g. treemacs
  +projectile flag) must use project.el equivalents.
- [2026-07] consult-flycheck removed. When :checkers ports, evaluate
  flymake + consult-flymake first before reinstating any flycheck glue.
- [2026-07] custom.el sanctioned to live in the config dir (explicitly
  git-ignored); the no-writes rule stands for everything else.
- [2026-07] local-package layout deferred - see "Local :repo packages".
- [2026-07] two latent bugs fixed while adding coverage: window-move-right/
  left recursed into themselves (rename-sweep collision - the doom.d
  originals called Doom's +evil/window-move-*; now windmove-swap-states-*)
  and window-cleanup+ used dash's ->> with dash not installed (now
  built-in thread-last).
- [2026-07] key-audit pipeline introduced (see "Key clusters"). SPC w
  pilot verdicts: evil-window-delete ported (SPC w d); J/K movers +
  split-and-follow pair ported renamed (windmove-based); winner C-u/C-r
  dropped (window-undo owns u); Doom's other-window and window-enlargen
  overrides dropped for stock evil equivalents; tear-off-window stays
  tab-window-detach; ace-window keys parked until :ui window-select.
- [2026-07] use-package-always-defer removed from init.el - the "Doom
  defers by default" premise was wrong (live Doom runs with it nil).
  Under always-defer t, every :after-only use-package block builds but
  never loads: vertico-posframe + which-key-posframe (the reported bug),
  all six vertico extensions (vertico-repeat's history saving was dead),
  evil-traces, yasnippet-capf, consult-yasnippet. Audited all blocks:
  everything else defers via explicit :defer/:hook/:commands/:after-call,
  so nothing became eager. Rule for ports: laziness must come from
  explicit keywords, never from a global default.
- [2026-07] scratch lib ported (general/autoload/scratch.el, from Doom's
  lisp/lib/scratch.el): doom/* commands -> bare names, doom-scratch-buffer
  -> scratch-buffer-create (built-in =scratch-buffer= collision), buffers
  now *scratch+* / *scratch+ (PROJECT)*, persistence under
  doom-data-dir/scratch/, projectile shim dropped,
  scratch-buffer-created-hook replaces Doom's misnamed
  doom-scratch-buffer-hook defvar (upstream ran the -created- name it
  never defvar'd). Unported: open-project/revert/delete commands,
  kill-on-hide hook (nothing referenced them). Trigger: SPC b s was
  bound-but-void - a class the key audit couldn't see because both sides
  bind identical symbols. The dump now emits :void for bound symbols
  without definitions; keydiff reports them as "broken" and only a
  :deferred verdict can park one.
- [2026-07] frames lib slimmed 300 -> ~100 lines (general/autoload/frames.el).
  Trigger: SPC z f bound-but-broken - frame-zoom-transient's font suffixes
  referenced never-vendored doom/*-font-size, a class invisible to the key
  audit (prefix bound + autoloadable, rot inside the layout). Kept:
  toggle-frame-full-height (y now workarea-derived, was a hardcoded mac
  offset; Linux behavior unverified until a real box),
  display-current-workarea, the mac menu-bar repaint helper. New:
  font-size-increase/decrease/reset over the default face height - wrapping
  built-in global-text-scale-adjust was rejected because its
  last-command-event dispatch misfires programmatically (from M-C-- it
  would increase; reset only works from a literal 0 key); text-scale-reset
  fixes the old s-0 suffix which ran text-scale-set = level 1, never 0.
  Dropped as WM-era leftovers: toggle-frame-maximized-undecorated,
  center-frame-horizontally, toggle-frame-fullscreen+,
  reset-frame-full-height, the 7 shrink/widen/resize helpers,
  place-frame-at-display-spot + hidden layout presets,
  display-workarea-width/height, screen-width-height (Doom config remains
  the reference if any revive). Rename sweep: C-+/M-C-=/M-C-- in
  bindings/config.el + a config.el docstring. tests/general/frames-tests.el
  covers the logic with stubs and walks the transient layout asserting
  every suffix command is fboundp - the regression net for this bug class.
  Follow-up: font-size-increment must stay a whole-point multiple - the
  initial 5 (0.5pt) made the first decrease press render as a no-op on NS
  (sub-point sizes snap to the nearest point, and only upward steps stay
  visible), reported as "decrease doesn't work". Now 20, matching Doom's
  2pt doom-font-increment feel; a spec pins the multiple-of-10 constraint
  and the GUI probe asserts rendered char height moves on decrease.
- [2026-07] key audit hardened against bound-but-broken-inside (the SPC z f
  class): the dump walks every bound command's parsed transient--layout and
  emits :void-suffix for undefined suffix commands; keydiff counts those as
  broken under the same verdict rules. Layouts only exist once the defining
  file loads, so keydump's boot force-loads autoloaded commands
  (dump-bindings-load-autoloads, nil in live sessions - the Doom-side dump
  stays conservative; keydiff only reads lab flags). KEYDUMP_REQUIRE gained
  org: built-in org autoloads omit commands transients reference
  (org-insert-link), a false :void-suffix otherwise. First run caught real
  rot invisible before (same symbol both sides): expreg-transient (SPC v -
  vulpea/markdown/clojure wrap suffixes) and window-transient (SPC w . -
  golden-ratio); both parked :deferred pending their modules. SPC w's four
  same-symbol ace-window/golden-ratio voids got their missing :deferred
  verdicts - the pilot cluster is clean again (0 open, 0 broken, 7 parked).
- [2026-07] SPC t cluster closed (8 verdicts). zen ported at Doom-feel
  parity (general/config.el): writeroom-mode with global effects nil and
  maximize-window t (doom.d override), text-scale 2.0 via
  writeroom-local-effects, visual-fill-column-adjust keeping the column
  fitted (also advised onto text-scale-adjust), mixed-pitch in
  org/markdown with the fixed-pitch face list trimmed to stock faces
  (doom-themes/solaire/org-ref ones dropped), the
  turn-off-writeroom-before-split-a advice carried over. +zen/toggle ->
  zen-toggle, an alias for writeroom-mode - lisp callers must pass
  'toggle since minor modes enable on a nil arg (GUI probe caught this).
  Zen-fullscreen variant and SPC t F toggle-frame-fullscreen both
  dropped: frame geometry belongs to the frame transient. dired-sidebar ported (follow-file t, window-fixed nil);
  fixed the ibuffer-sidebar :commands "siderbar" typo next door.
  doom/toggle-indent-style -> toggle-indent-style
  (general/autoload/misc.el, with specs). doom-big-font-mode and
  evil-goggles bindings dropped. SPC t f rebound flymake-mode ->
  flycheck-mode, parked on :checkers; supersedes the earlier "evaluate
  flymake + consult-flymake first" note - the user runs flycheck.
- [2026-07] git module ported (magit, forge, evil-collection-magit glue,
  git-link, gh-notify, code-review (ag91), consult-gh, git-auto-commit-mode,
  browse-at-remote, github-topics, remoto + explicit request/deferred - the
  autoloads' own deps, transitive-only in doom.d). Local-dev flow decided at
  this first consuming port: own packages declare plain GitHub recipes;
  =local-checkout-recipe= on =elpaca-recipe-functions= (init.el) redirects
  any =local-dev-packages= entry to its ~/GitHub/agzam checkout when present
  (build-in-place), other machines just clone from GitHub. github-topics
  extracted from the doom.d submodule to ~/GitHub/agzam/github-topics
  (unpushed dce3530 preserved; origin re-pointed, push pending). Fixes over
  doom.d: (after! 'evil-escape) / (after! 'evil-collection) in the
  code-review block quoted the feature - waits on feature =quote=, never ran
  (fixed unquoted); forge-select-issue rebuilt on forge-read-issue
  (forge--format-topic-choice/forge-ls-issues gone from forge);
  forge-visit-topic-via-url now requires ghub-legacy (ghub moved
  ghub-fetch-issue/-pullreq there, nothing else loads it); the magit-dispatch
  ("o" "Dummy" nil) shim dropped - modern transient hard-errors on
  nil-command suffixes (live probe caught it), replaced by inserting
  ("N" "Forge" forge-dispatch) ourselves in forge :config since
  evil-collection-magit's o->' dispatch remap still breaks forge's own
  anchor (root cause of the tarsius-era doom.d workaround);
  bug-reference-mode org/markdown hooks moved out of =after! bug-reference=
  (registration waited on a load that only those hooks trigger); stale
  =:after emacsql-sqlite-builtin= forge gate dropped (feature gone, emacsql
  4.x merged backends). Slims: +magit-display-buffer-fn + in-direction
  helper (dead in doom.d - +magit-open-windows-in-direction never defined)
  and the stale-buffer machinery dropped - doom-defaults' doom-auto-revert
  covers buried buffers, magit-quit-all calls doom-auto-revert-buffers-h for
  visible ones; consult-gh config + autoloads skipped (config block is
  :disabled in doom.d; autoloads were embark glue for it); magit-gitflow
  after! block (package never installed), straight-install-github-package
  (straight-ism), magit-transient-unblock-global-keys (call commented out),
  github-topics jinx hook (writing module), magit-blame turn-off-evil-org
  hook (org module) all dropped. Rewrites: magit-create-branch-friendly-string,
  forge-select-issue, magit-worktree-branch-from-issue without dash/s
  (verified against the dash originals live; batch specs pin 7 cases).
  vc-git-link{,-homepage,-kill,-kill-homepage} implemented fresh over
  browse-at-remote (bindings-tree names; the v3 Doom install has no :emacs
  vc module source to vendor). :tools magit stays OUT of
  doom-modules-enabled - doom.d comments it out too (custom git module
  replaces it); (:custom git) added. forge/code-review sqlite files
  quarantined in doom-compat.el; doom.d's transient-file :init block already
  covered there. doom.d rot found, not lab bugs: +forge-select-issue and
  forge's dispatch insertion are broken in doom.d today.
- [2026-07] org module ported, CORE SLICE: org (elpaca-menu-org builds
  10.0-pre, matching live Doom's org 10; org-version/loaddefs generated -
  doom.d's straight :pre-build fake dissolved; elpaca-menu-org-make-manual
  nil, makeinfo is absent on CI and Doom never built the manual either),
  org-roam(+ui; extensions flattened MELPA-style via :files
  (:defaults "extensions/*") - doom.d's manual build-path hack dissolved,
  org-roam-ui hard-requires org-roam-dailies), vulpea trio +
  consult-vulpea (explicit GitHub recipes; consult-vulpea is
  fabcontigiani's, not d12frosted), evil-org (hlissner fork),
  org-modern-indent, org-appear (org-9.7-fixes branch). Autoload files
  slimmed to the core slice: org-tables/org-babel reduced to their single
  survivors (org-table-previous-row, org-clear-babel-results-h - folded
  into autoload/org.el); org-capture frame infra, org-refile/org-avy,
  org-export, org-link's gif/Doom-link helpers, contrib-*, verb, anki all
  wait with their packages. Renames: +org/foo -> org-foo tree;
  +org-link-make-description-function -> org-link-make-description-fn
  (bare rename would shadow the defcustom's name). Dropped as dead in
  doom.d: org-roam--link-to, org--insert-selection-dwim, org-link-parse,
  vulpea-insert+, :commands org-roam-buffer-toggle-display (v1 API).
  doom.d rot fixed on port: get-gh-item-title's repo regex used a
  =[:graph:]=-class match (eats "/") - every blob/commit link fell into
  the repo branch and the file-in-branch arm was dead (specs pin all
  three);
  the vulpea display-buffer-alist entry wrapped regexp-quoted strings in
  rx literals - matched literal backslashes, never fired;
  org-goto-bottommost-heading leaked a global =currlevel=;
  org-goto-datetree-date missed (require 'org-datetree). No quoted-after!
  instances this time. Lab-side deviations: flycheck-disable-h is
  fboundp-guarded - unguarded (flycheck parked on :checkers) the void
  call aborted the tail of org-mode-hook, silently killing
  org-modern-indent/cape-org-capf/bug-reference (live probe caught it);
  localleader map! hoisted out of doom.d's per-buffer org-init-keybinds-h;
  its (:when (modulep! :completion vertico)) guard dropped - the lab
  registry says :custom completion, so the guard silently pruned
  consult-org-heading; org-noter ("n") and org-cliplink ("i L")
  localleader bindings dropped until pdf/long-tail; yt links fall back to
  browse-url while mpv-open+ waits on web-browsing; ox-gfm's
  org-export-with-toc nil folded into org config (ox-gfm was :after org -
  same effective timing); magit-blame's turn-off-evil-org-h lives in
  evil-org's :config, not the git module - void-safe when evil-org never
  loads; org-roam-ui-on wraps enable in with-demoted-errors (the parallel
  Doom session may hold port 8088). insert-dash + insert-bracket-pair
  stay with the writing module - org's =[ SPC= and config.el's ="-"=
  insert-state bindings are void until it ports (doom.d-verbatim; note
  the "[[" vulpea-insert prefix leaves no plain-bracket escape in org
  insert state until then). org-wrap-in-block stays in general -
  expreg-transient depends on it and general must not depend on org
  (the "move or leave" decision delegated with this port's instructions).
  vulpea keeps doom.d's :commands (vulpea-journal+
  vulpea-journal-note): -note isn't autoloaded upstream, loading vulpea
  chains vulpea-ui -> vulpea-journal through the :after gates, and
  use-package's (unless (fboundp ..)) wrapper protects the module loaddef
  for vulpea-journal+. Quarantine additions: org-id-locations-file,
  org-persist-directory (isolate from the Doom session's XDG default -
  different org builds), org-publish-timestamp-directory,
  org-clock-persist-file, org-roam.db + vulpea.db (lab builds fresh dbs;
  org-roam autosync stays off like doom.d - run org-roam-db-sync once for
  the UI graph). Test gotchas pinned in suites: never buttercup-spy org
  edit commands - every org-mode call runs org-fold--advice-edit-commands,
  whose advice-add rewrites the advised symbols' function cells and
  orphans the spy bookkeeping (org-return, org-self-insert-command, ...;
  use cl-letf); buttercup :var is useless for special vars (binding is
  live while the suite builds, not when specs run). KEYDUMP_REQUIRE
  gained vertico: config.el's (:after vertico) bindings dumped as
  false-open SPC r entries. Restored: vulpea-insert in expreg-transient
  ("C-c i"). Verified: 239 specs 0 failed (default + transient-0.7.2
  runs), SMOKE-OK 86 packages 0 failed/blocked, live probe (vulpea
  journal end-to-end incl. drawer-lint save, hook/keymap asserts, expreg
  suffix walk), keydiff 0 regressions - SPC o's roam/org broken cleared,
  SPC r closed.
- [2026-07] expreg-transient fixed for real - the parked "core works"
  note was wrong. Its bypass section's :setup-children calls
  transient-bypass-keys, which lived in doom.d's unported
  lisp/sexp-transient.el: SPC v errored at open, invisible to the audit
  (setup-children rot, a third blind class after :void and
  :void-suffix). Ported the engine into general/autoload/expreg.el
  (move, don't duplicate, when sexp-transient restores) with
  transient-layout-keys - a dialect-aware rewrite of the doom.d helper
  whose cl-typep/oref walk never matched spec-list layouts, so the
  key-conflict guard had been dead in Doom all along. Alive again, it
  caught the Magit section colliding with bypass "s"/"x"; that section
  dropped (unreachable in practice). Slimmed like frames: vulpea-insert,
  the browser-url inserters (+ helper defun), Markdown and Clojure
  sections dropped until their modules port; bypass "%" evilmi ->
  stock evil-jump-item. org-wrap-in-block ported early into
  general/autoload/misc.el (from doom.d org/autoload/custom.el, dedupe
  when org lands) so "; q"/"; c" work. Tests: dual-dialect layout
  walker now shared via tests/helper.el, exact-set layout spec as the
  rot net, bypass-engine + org-wrap-in-block specs; a boot probe opened
  the transient live and walked live suffixes (bypass children
  included) - all defined.
- [2026-07] colors module ported (circadian, ag-themes LOCAL-REPO,
  spacemacs-theme, base16-theme, doom-themes, rainbow-mode, beacon;
  ef-themes stays out - disabled upstream too). ag-themes extracted from
  the doom.d submodule to ~/GitHub/agzam/ag-themes.el (af440d2 already on
  GitHub, nothing unpushed; origin re-pointed, local-dev-packages entry;
  elpaca's in-place build left the checkout clean). Recipe excludes the
  repo's stale straight-era *-autoloads.el/-pkg.el - elpaca regenerates
  autoloads from cookies, including the custom-theme-load-path cookie in
  ag-themes.el. Theme loading: circadian owns it, hooked on
  doom-init-ui-hook instead of window-setup-hook - elpaca activates
  packages after window-setup on cold boots, so a :hook registration
  there would never fire. tty gets themed exactly like doom.d in
  practice: the doom.d display-graphic-p guard (doom-theme nil) only
  suppressed Doom's own pre-circadian load, machinery the lab doesn't
  have. Cycle helpers anchored on (car custom-enabled-themes) - doom.d
  rot: they keyed on doom-theme, which circadian's timed switches never
  update (no load-theme advice in the live install, ground-truthed), so
  the ring anchor went stale after every scheduled switch. Renames:
  colors/cycle-themes-down/up -> cycle-themes-down/up,
  colors/load-next/prev-theme -> load-next/prev-theme; the duplicated
  next/prev bodies merged into load-cycled-theme. The transient prefix
  lives in autoload/cycle-themes.el (lazy dir, not the eager single
  autoload.el - that would require built-in transient at boot, before
  elpaca activates the real one; the loaddefs subprocess handles the
  cookie). doom-defaults' window-divider-mode hook removed (port of the
  doom.d remove-hook!). beacon guard (featurep :system 'macos) -> (eq
  system-type 'darwin) - installed but off on macOS/tty. Coverage:
  ring/cycle logic + wrapper composition in
  tests/colors/cycle-themes-tests.el (circadian provide-stubbed); theme
  activation, circadian slot pick, real-ring round-trip, mode-line-active
  :inherit survival, beacon/rainbow spot checks live in the tty probe
  (all green); GUI parity rides on daily use once the cord cuts.
  Housekeeping cleared same sitting: github-topics dce3530 pushed
  (upstream now tracks), one-time org-roam-db-sync ran headless against
  the quarantined db (2475 nodes / 1447 files).
- [2026-07] modeline module ported (doom-modeline; elpaca pulls
  nerd-icons). Scrutiny over parity: doom.d stamped an `agcustom'
  modeline buffer-locally from window-state-change-hook, re-running
  doom-modeline-def-modeline on every window change behind a
  local-modeline guard; the lab redefines doom-modeline's `main' once
  (autoload/segments.el, apply-custom-modeline) - the default value
  styles every buffer, doom-modeline's own special modelines (pdf, ...)
  still apply buffer-locally on top. Ground-truthed live before
  deciding: the default mode-line-format there is stock `main'
  (agcustom only ever existed as per-buffer stamps), and persp-mode
  runs in neither config - persp-name segment dropped (workspace-name
  is the tab-bar-port candidate). Dead sets dropped:
  doom-modeline-mu4e nil (sets the default; mu4e never installed),
  doom-modeline-modal-icon t (default, and no modals segment in the
  layout), the :unless (modulep! :ui modeline) guard (never true in
  the lab registry). Deliberate pins kept even where they match
  current defaults (modification/state icons, bar-width) - they guard
  upstream default drift. Anti-flash blanking hoisted from :init to
  module top level: elpaca evaluates use-package forms after
  after-init-time is set, so doom.d's guard made it dead code in this
  boot order. inhibit-compacting-font-caches moved out of setopt
  (plain C defvar, not a defcustom); colors' beacon block switched
  setq -> setopt the same sitting (all five are defcustoms). False-rot
  lesson pinned: doom-modeline segments resolve via
  doom-modeline--fn-alist AND --var-alist (variable-bodied segments
  like pdf-pages live in the latter), so fboundp probes of
  doom-modeline-segment--* mislead; doom-modeline--prepare-segments
  hard-errors on unknown names, which makes the probe's
  both-registries walk the layout rot net. Coverage:
  apply-custom-modeline wiring + dead-segment reintroduction pins in
  tests/modeline/segments-tests.el (doom-modeline provide-stubbed);
  probe asserts mode enabled, `main' as default format, segments
  registered, render non-empty, font-height override 1, colors theme
  intact. keydiff unchanged - no bindings in this module,
  0 regressions.
- [2026-07] dired module ported (treemacs, treemacs-evil,
  treemacs-icons-dired, dired-imenu, dired-subtree, dired-narrow,
  dired-preview + dired-sidebar relocated in from general - its doom.d
  home was this module; setq -> setopt on the move, ibuffer-sidebar
  stays in general). Smoke 107 packages (+14: the treemacs dep tree
  pulls dash/s/ace-window/pfuture/ht/hydra/cfrs). Not ported:
  treemacs-projectile (projectile dropped config-wide) -
  treemacs-project-toggle+ rebuilt on
  treemacs-add-and-display-current-project-exclusively with an explicit
  (require 'treemacs) (the visibility fns it dispatches on aren't
  autoloaded), dired-jump-find-in-project renamed (+ stripped) with
  project.el root resolution; lsp-treemacs parked on the lsp module.
  Rot dropped after ground-truthing: the winum unignore glue - upstream
  renamed treemacs--buffer-name-prefix -> treemacs-buffer-name-prefix,
  so doom.d's treemacs-mode-hook fn void-variable'd on every treemacs
  buffer and its ".*Treemacs.*" removal targeted a pattern winum no
  longer carries (treemacs's own compat glue adds
  " \\*Treemacs-Buffer-" instead); live behavior = treemacs windows
  unnumbered, preserved here, only the s-N treemacs-mode-map loop was
  alive and ported. Also dropped: the darwin gls block (Emacs 31's
  insert-directory-program standard value resolves gls natively; the
  effective "-aBhl --group-directories-first" switches kept),
  dired-kill-when-opening-new-dired-buffer nil (set-to-default), the
  direx branch of buffer-with-dired-item (direx never in packages.el).
  Rewrites: dired-split-action defmacro -> dired-open-item-in-split fn
  + cmd! (an autoloaded macro would drag its file in at config
  macroexpansion); dired-preview deferred (doom.d demand-loaded it;
  M-x-only entry, no bindings, no writers). Kept deliberately: the
  dired-mode-hook map! re-application - evil-collection-dired's lazy
  setup binds "o" after any config-time registration, the hook is the
  user-wins mechanism (probe pins "oj" -> split action);
  turn-off-evil-matchit-mode fboundp-guarded until tree-sitter (org
  flycheck precedent); the zoxide hook pre-renamed
  (add-to-zoxide-cache) behind its :custom search guard. Evil state ?T
  via doom-evil-state-alist; treemacs persist pair quarantined in
  doom-compat.el (audit found no other writers in the suite).
  Coverage: subtree/rsync/project batch suites (treemacs
  provide-stubbed, cl-letf spies); probe covers subtree round-trip
  incl. the icons advice in tty, sidebar toggle, treemacs
  toggle/state/s-N/hl-line, quarantine paths, preview deferral pin,
  colors + modeline regression guards - all green. keydiff: SPC p T
  resolved by exact match (:ported pin), SPC p t :improved; ace-window
  arriving as a dep flipped the root-config W/D/M bindings live
  (:ported - they were bound ahead, waiting), C-S-w/C-c :dropped
  (reachable at SPC w M/D; the C- spellings were window-select
  artifacts). 154 open / 95 broken / 5 parked / 32 resolved /
  0 regressions. New lab-only class surfaced 3 SPC h rows - doom-side
  structural drift (h b / h d became prefixes there, h C-h unbound),
  SPC h cluster territory, untouched.
- [2026-07] smartparens base + eros landed (the elisp sitting's opener).
  sp core vendored from Doom modules/doom/compat/+smartparens.el into
  general/config.el, right above the user pair-rule block that was inert
  since the general port: language rule blocks (cc/ruby/swift/haskell/
  python/ml/markdown) parked until those modules port, Doom's quote/brace/
  lisp-( rules omitted - the user block re-specifies them with its own
  :unless lists; sly-mrepl line dropped. Defused on the way: the user block
  referenced doom-disable-show-paren-mode-h, defined nowhere in the lab -
  vendored and renamed disable-show-paren-mode-h. eros rides
  emacs-lisp-mode-hook (Doom :tools eval +overlay equivalent), lives in the
  elisp module beside its consumers. Related fix surfaced by the scratch
  work: doom-defaults set require-final-newline with plain setq - the var
  is auto-buffer-local when set, so only the buffer current during init
  (the stillborn *scratch*) ever got it; setq-default now, test asserts
  default-value (it read the accidental buffer-local t before).
- [2026-07] elisp module ported (macrostep and eros added explicitly -
  doom.d got them from Doom's :lang emacs-lisp and :tools eval; a,
  elisp-format with an explicit github recipe, deferred behind :commands -
  doom.d demand-loaded it against the preloaded elisp-mode).  let-plist +
  evilify-edebug folded into module autoload/ per user decision instead of
  the planned ~/GitHub extraction: they were in-module :local-repo packages
  only because of straight-era load-order weirdness; a cookied defmacro in
  the module loaddefs also heals general's bare let-plist uses (url.el x4,
  misc.el) at call time - the "latent dep" note below is settled.  Renames:
  sp-eval-current-sexp -> eval-current-sexp (user fn, not smartparens's),
  with-editor-eval -> eval-current-with-log REBUILT (its body called
  eval-current-form-sp, void in live Doom - rot; the name also collided
  with the real with-editor package), edebug-eval-current-form-sp ->
  edebug-eval-current-sexp.  Rot fixed on port: datetime->timestamp passed
  a raw number to message (wrong-type error live) with HZ-dependent tick
  math - float-time now; elisp-fully-qualified-name passed a hardcoded
  symbol to find-function-library (copy-paste slip) - symbol at point with
  buffer-file fallback; +last-known-elisp-buffer was setq'd into the void -
  defvar'd; create-package read magit-clone-default-directory unguarded
  (void until magit loads) - bound-and-true-p.  templates/ asset relocated
  into modules/elisp/templates/.  Root layer gained helpful (Doom-core
  there; its SPC h a/f/v + mode-map bindings were already carried, void),
  golden-ratio + lorem-ipsum (root packages.el leftovers - golden-ratio
  closed the last two SPC w parked entries, the cluster is done) and
  consult-symbol pulled ahead of the search module (SPC h h, user-flagged).
  Coverage: 6 batch suites (let-plist macro expansion, sharp-quote/eval
  wiring, messages guards, datetime round-trip + fqn, hermetic
  create-package scaffold, evilify-edebug wiring); probe covers the eros
  overlay render (eros insta-clears without this-command outside the
  command loop - probe artifact class, documented there), macrostep
  round-trip, cold-session let-plist macro-autoload, localleader tree,
  messages window cycle, helpful + describe-function advice routing,
  edebug evil state.  Keydiff: SPC h broken 5 -> 1 (helpful trio +
  consult-symbol), doom-internal bulk verdicts (27 :dropped - the SPC h d
  self-introspection tree can never mean anything without Doom), SPC h
  47 -> 18 open; the b subtree, describe-* drift and the profiler toggle
  wait for the dedicated SPC h sitting. 125 open / 91 broken / 4 parked /
  60 resolved / 0 regressions.
- [2026-07] the persistent scratch replaced the built-in *scratch*
  (user request: two scratch buffers even under Doom, the built-in dead
  weight).  The global persistent scratch now IS *scratch* (project ones
  *scratch (NAME)*; the + suffix existed only to dodge the built-in name);
  startup-scratch-buffer buries the stillborn pristine *scratch* and
  becomes initial-buffer-choice - startup lands in the persistent buffer;
  a get-scratch-buffer-create :override covers the choke point every
  built-in path funnels through (scratch-buffer command, emacsclient
  no-args, fallbacks), so a killed scratch resurrects with its persisted
  state.  A modified foreign *scratch* gets adopted (content kept,
  persistence attached), not killed - pinned in tests.  Probe: startup
  buffer is the persistent *scratch*, exactly one scratch in the list,
  choke point returns it, kill -> resurrect restores content.  Batch tier
  keeps a pristine *scratch* invariant around the suite (killing it
  changed the current buffer for later suites - that unmasked the
  require-final-newline slip above).
- [2026-07] SPC h cluster closed.  Doom's help-map surgery (config/default
  config.el) vendored into bindings/config.el, pruned: the b subtree went
  live on built-in which-key (b b describe-bindings + b f/i/k/m/t
  which-key-show-*), describe-* drift matched key for key (' C-c C-k C-l
  RET F), T got toggle-profiler - a renamed doom/toggle-profiler vendored
  into elisp/autoload/profiler.el, which also absorbed the misc.el
  profiler-report helpers (all profiler stuff in one file, user call).
  Deliberate divergences (:improved): stock d (apropos-documentation, so
  doom's h A stays unbound), stock P (describe-package - find-library
  covered at h p f), stock C-h (help-for-help; doom unbinds it for
  which-key paging), W on woman directly (+default/man-or-woman resolves
  to woman on macOS anyway), r r reload-config.  h t load-theme and h O
  +lookup/online :dropped (themes live at SPC T; O was parked on lookup -
  never used, figure out later).  Broken fix: h p d bound void
  doom/describe-package - stock describe-package now.  Coverage:
  profiler-tests suite (toggle order pins report-before-stop), probe
  checks all 19 binds via lookup-key.  Keydiff: SPC h 18 -> 0 open, off
  the diff table; 107 open / 90 broken / 3 parked / 71 resolved /
  0 regressions / 0 lab-only.
- [2026-07] tree-sitter module ported (registry slot between dired and
  org, doom!-faithful).  Emacs 31 gate on the treesit block - CI's 30.1
  has none of treesit-enabled-modes/treesit-ensure-installed, and locally
  the boot-time ensure loop would y-or-n-p ('ask policy) inside every pty
  probe/smoke run - grammar ensure now rides with-eval-after-load of its
  consumers (yaml-pro, mermaid-ts-mode).  treesitter-context kept Doom's
  always-defer semantics with an explicit :defer t (lab sets no
  use-package-always-defer); its fold-all advice body moved to
  modules/tree-sitter/autoload/fold.el for the batch tier - 5 specs pin
  the close-all/open-all walk and the passthroughs.  evil-matchit
  (redguardtoo is upstream, not a fork) on doom-first-file; going live
  self-activated dired's evil-matchit-off-h fboundp guard - probe:
  matchit on in file buffers, off in dired, treesitter-context still
  deferred at boot.  clojure-ts-mode + mermaid-ts-mode (:files) are
  install-only stubs (elpaca installs strictly via :ensure).  Smoke
  116 -> 121 packages, 0 failed/blocked; keydiff untouched (107 open /
  90 broken / 3 parked / 0 regressions); the agzam in-place checkouts
  stayed clean through the builds.
- [2026-07] shell module ported (registry slot after org).  Adjudicated
  drops, all git-resurrectable: eshell-aliases-file setq (target never
  existed - Doom :term eshell's default alias alist folded in as the
  effective set, minus bd/cdp whose targets aren't ported), Hyprland env
  refresh (Linux-only), mise (installed-but-inert in doom.d), yuck-mode,
  shrink-path (prompt pwd falls back to abbreviate-file-name).  :term
  eshell fold-in: aliases + write-suppression advice + prompt (renamed
  eshell-default-prompt-fn, faces eshell-prompt-pwd/-git-branch).
  shell-pop-in-project-root rebuilt on project.el, same name; eshell-here
  is a thin project-aware opener; the vendored void eshell-toggle row and
  the invented :term ghostel block left the bindings tree (those ghostel
  commands never existed upstream - real entries are ghostel/
  ghostel-project, M-x surface like doom.d).  zoxide-find pulled ahead
  from the unported search module (relocates with it).  Names born
  plus-free per the no-plus-affix rule: eshell-clear-buffer,
  eshell-export-output.  eshell-atuin installs unconditionally, activates
  behind executable-find (smoke determinism); mxp ansi hook kept with a
  provenance comment (~/GitHub/agzam/mxp drives it via emacsclient).
  kkp's tty-setup hook fires in pty probes (CSI u query visible in raw
  tty output) but *Warnings* stays clean - no gating needed.  Coverage:
  shell-tests + eshell-tests suites (16 specs; the find-file-visit-truename
  + /private/var symlink gotcha pinned in the fontify specs).  Smoke
  121 -> 127 packages, 0 failed/blocked.  Keydiff: 107 -> 102 open,
  90 -> 88 broken, 71 -> 74 resolved, +1 parked (SPC o e deferred to
  elfeed), 0 regressions; SPC ' / SPC " clusters off the table.
- [2026-07] plus-affix sweep: the trailing-+ idiom is dead (AGENTS.md
  naming rule replaced).  Renames, refs and suites updated:
  window-cleanup, treemacs-project-toggle, code-review-browse-pr (bare
  names were free), magit-clone-simple, dired-remove-subtree,
  dash-docs-ensure-docset / -ensure-user-docset /
  -unofficial-docsets-versioned (bare names shadow the package fns they
  wrap - the dash-docs ones are :override advice targets),
  org-roam-ui-in-browser, open-journal + journal-template (the
  vulpea-journal package owns that namespace).  Void-bind rot fixed on
  the way: SPC i y binds consult-yank-pop directly (doom's
  +default/yank-pop is a consult dispatcher; the row was void
  yank-pop+), C-SPC embark-preview and global M-r eval-whole-buffer are
  plus-free placeholders (providers: the custom embark and :tools eval
  ports; eval-buffer+ was defined nowhere, doom.d included), mpv-open
  guard and open-gptel binds renamed ahead of their module ports.
  Exempt: info+ (upstream package name), doom.d symbol names in
  provenance comments and long-tail lists, the deliberate
  eshell-clear+/eshell-export-output+ negative specs.  Flagged,
  untouched: journal.el's vulpea-journal--* private helpers squat the
  package namespace without + affixes.  Verification: 310 specs green,
  smoke 127 / 0 failed/blocked, probe sweep-residue empty (no +-affixed
  fboundp survives boot), keydiff 102 open / 87 broken / 78 resolved /
  0 regressions (the SPC i y broken row closed; SPC w c, SPC o r W,
  SPC p T re-verdicted :improved).
- [2026-07] ai module ported - the last tool the porting workload needs,
  lab is now self-hosting for migration sittings.  Keep-set: gptel
  (AGENTS.md-as-directive via function value, ollama/copilot backends,
  model persistence through custom.el, quadrant display rules), eca
  (buffer renames, archive-on-finish + eca-continue-from-file,
  flag-and-fork, auto-trust-for-plan, eca-reauth), mcp + lazy tool
  registration (schemas parsed straight out of the harness .bb files),
  gptel-agent (skills from ~/.config/eca/skills/, MCP-cats/AGENTS.md
  injection advice), gptel-anthropic-oauth as default backend, piper
  (no mxp code coupling - clipboard->tts stays xsel/Linux).  Dropped
  (commented/inert in doom.d, git-resurrectable): whisper, gptel-quick
  (package; the local gptel-quick-question-buffer defun stays),
  ob-gptel (live but cut by choice), llm-tool-collection, ragmacs,
  shell-maker, acp, agent-shell, claude-code.  LOCAL-REPO deviation:
  gptel-tools + gptel-anthropic-oauth vendored INTO the tree at
  modules/ai/ - no upstream remotes exist, so the ~/GitHub extraction
  pattern would break CI cloning; elpaca builds in place from repo
  subdirs (repo-root worry unfounded, smoke-proven).  gptel-tools stays
  installed-not-loaded like doom.d; its emacs floor lowered 30.2->30.1
  (CI).  Harness-contract renames, collision-checked against mcp.el
  and verb-fronted where they read like package API (setup.bb only
  writes the files these read - elisp names never called externally):
  eca-agents-md-content, mcp-servers-from-eca-config,
  register-mcp-tools-lazy, ensure-mcp-server, lazy-mcp-tool-fn,
  mcp-schema->gptel-args, extract-tool-defs-from-bb; plus gptel+ ->
  open-gptel, gptel-clear-buffer+ -> gptel-clear-buffer,
  +gptel-improve-text* -> gptel-improve-text*.  parseedn explicit now
  (doom.d rides the clojure module's dep tree); .bb reads guarded, so
  missing harness/CI no-op.  Quarantine grew: anthropic-oauth +
  copilot token caches and crowdsourced-prompts -> cache dir,
  eca server binary/version -> data dir (one-time re-auth and server
  re-download in lab; sharing Doom's tokens.el invites the
  refresh-token rotation race eca-reauth exists for).  insert-comma
  pulled ahead of writing (jinx.el); evil-markdown-mode-map refs
  dropped (no evil-markdown here; markdown-mode already present -
  code-review dep, eca pins it); open-gptel gained a boundp guard
  (doom's gptel+ errors when called before gptel ever loads).
  Verification: 338 specs (+28, tests/ai/), smoke 127 -> 135 packages
  (+6 keep-set, +parseedn/parseclj), 0 failed/blocked, probe green
  (rows resolve, ai packages deferred at boot, boot-registered eca
  advice pending, quarantine paths). Keydiff 102 -> 93 open / 87
  broken / 78 -> 81 resolved / 0 regressions - SPC x g subtree +
  SPC o c g off the open list.
- [2026-07] web-browsing module ported.  Keep-set: eww glue (shr
  setopts, rename hook/advice, mode-map + localleader), browser-hist,
  consult-hn (+ consult-hn-transient require, reader fn, s-return),
  reddigg + hnreader (darwin fetch-via-browser, show-all hook, quadrant
  advice), navegosa, rfc-mode, subed, mpv helpers + transient,
  xwidget helpers (inert without an xwidgets build), yt.el, readable/.
  Dropped (2026-07 decision, unused, git-resurrectable): elfeed (SohumB
  fork) + elfeed-org/elfeed-tube/elfeed-tube-mpv, yeetube;
  autoload/elfeed.el whole, elfeed.org asset stays in doom.d; lab
  SPC o e bind removed, ledger row :dropped.  mpv PROMOTED to an
  explicit package (was transitive via elfeed-tube-mpv); mpv-transient
  lost its two elfeed-tube-mpv suffixes ("f" follow / "w" where - void
  after the drop), layout pinned by spec + probe.  Renames (plus-ban):
  +eww-{jump-to-url-on-page,open-in-other-window,copy-current-url,
  increase-font-size,decrease-font-size} -> eww-*, +process-external-url
  -> process-external-url, +browse-url -> browse-url-externally,
  mpv-open+ -> mpv-open (lab C-x m + org yt: placeholders expected it),
  subed-mpv-play-from-file+ -> subed-mpv-play-media (naive de-plus
  collides with subed's own play-from-file), shr-put-image* ->
  shr-put-sliced-image, +navegosa-...-space-a -> navegosa-...-space-a;
  file reddig+hnreader.el -> reddigg-hnreader.el.  doom.d
  writing/embark/search configs still reference the old names - rename
  at their ports (completion's dash-docs.el was already lab-renamed).
  LOCAL-REPO audit: consult-hn/reddigg/navegosa in sync with GitHub
  (navegosa README-only dirty), doom.d's browser-hist submodule 2
  BEHIND origin/main, nothing unpushed anywhere; browser-hist.el +
  consult-hn extracted to ~/GitHub/agzam (fresh clones, user-approved);
  reddigg recipe pins :branch fetch-via-browser (fork features -
  fetch-via-browser/chromium-warmup - live only there, master is
  upstream's); all four in local-dev-packages.  readable/ asset moved
  in-tree (extract.cljs + package.json), but npm install/node_modules
  QUARANTINED to a doom-cache-dir/readable/ workdir (files copied there
  before install); fixed doom.d's npm bootstrap - shell-command was
  handed format args without format, so it never ran.  Guards:
  jinx-mode call now fboundp-gated (writing unported), org-cliplink
  fboundp-gated in browser-insert-link-from-active-tab's linux branch
  (org long-tail), browser-tab-act keeps its embark-ephemeral-act call
  runtime-void until embark ports (user verdict: port as-is).
  navegosa deferred via :commands (eager in doom.d, no reason found).
  Latent doom.d bugs fixed: subed-view-plain-text cookie typo
  ;;;;;###autoload (void localleader ",v" bind), eww--rename-buffer's
  (eq "" title) never true for fresh strings (untitled pages now fall
  back to the url).  consult-line-collect-urls requires consult at
  call time (gptel-log-find precedent).  expreg-transient browser-url
  inserters restored (Org rows + expreg-transient--insert-browser-url).
  Test-hygiene fix while landing: buttercup :var slots are lexical, so
  tests/git/bug-reference-tests.el leaked its modified
  bug-reference-bug-regexp into later suites - rewritten with dynamic
  lets (buttercup already warned).  Verification: 372 specs (+34,
  tests/web-browsing/), smoke 135 -> 144 packages (+8 keep-set,
  +promise via hnreader/reddigg), 0 failed/blocked, probe green (binds
  resolve incl. SPC o r C-b, packages deferred at boot incl. navegosa,
  mpv-transient layout void-free, no elfeed/yeetube/+eww residue,
  quarantine paths). Keydiff 93 -> 88 open / 85 broken / 82 resolved /
  parked 4 -> 2 / 0 regressions - SPC b h/t/l/y/a/e + SPC b y +
  SPC o r C-b closed, SPC o e re-verdicted :dropped, SPC j x un-voided
  by the xwidget port.  Post-push amendment: hnreader + reddigg
  DISABLED (blocks commented in config.el) - both forks are currently
  broken (user verdict, fix later); module helpers + specs stay live
  (package-independent), consult-hn's reader and
  process-external-url's hn/reddit branches runtime-void meanwhile;
  smoke 144 -> 141.  The CI smoke hang itself (600s watchdog, one
  order never terminal, twice) turned out to be navegosa.el: the repo
  was PRIVATE, so elpaca's anonymous https clone sat at a git
  credential prompt the pty swallowed - invisible locally because
  local-dev-packages builds in place.  Repo made public (user call);
  GIT_TERMINAL_PROMPT=0 added to the smoke + keydump pty envs so a
  private/unreachable remote now fails the order visibly instead of
  hanging the boot.
- [2026-07] embark module ported (embark + embark-consult, plain MELPA;
  registry slot completion < embark < colors in both lists).
  consult-gh-embark glue stays out (git-port verdict).  embark-consult
  wiring is package-only: embark.el auto-requires it once consult
  loads; the (require 'consult) in :config kept (probe sees
  embark-consult arrive with embark).  Bind-at-startup fix over doom.d:
  [remap describe-bindings] embark-bindings + minibuffer C-c C-;
  embark-export and C-c C-e vertico-embark-export-write moved to :init
  - doom kept them inside embark's :config, where only a live session
  with embark already loaded resolves them; C-; embark-act was already
  root-layer, SPC a embark-act added to the root leader tree gated on
  :custom embark ("Actions" desc kept) - closes the SPC a singleton
  (ledger :ported, keydiff 88 -> 87 open, 0 regressions).  Minibuffer
  reconciliation (user verdict: union, root wins): root's C-c C-s
  embark-collect kept, doom's duplicate C-c C-l collect dropped.
  Reference updates to lab renames: eww-open-in-other-window,
  browse-url-externally, mpv-open, window-(v)split-and-follow, the
  link-* converter family, fetch-github-raw-file, forge-browse-topics,
  google-translate-es->en / en->es (text-action block, still gated on
  :custom writing).  magit-clone-regular+ -> magit-clone-simple: the
  already-renamed git-module port, discovered at mapping time - no
  double port.  +embark-project-search -> embark-project-search rebound
  to (consult-ripgrep nil target) (user verdict; +vertico-file-search
  waits on the search module).  Renames (plus-ban):
  +embark-{setup-url-types,target-url-at-point,target-org-block,
  collect-outline-cycle} -> embark-*, embark-preview+ -> embark-preview
  (completion's C-SPC consumer expected it), +search-rfc-number-online /
  +browse-rfc-number-at-point / +edebug-instrument-symbol de-plussed
  (no elisp-module collision).  +embark-url-config -> embark-url-config
  defcustom with :group 'embark (the old 'embark-url-config group never
  existed); +embark-url-patterns was a setq on a free variable - proper
  defvar added (autoload/embark.el).  embark-open-externally+ dropped
  (byte-identical wrapper); embark-file-map "x" binds the original.
  awesome-switch-to-prev-app-and-type "x p" row dropped - hammerspoon
  era, void in doom.d too.  embark-ephemeral-act's (define-key
  minibuffer-local-map "RET" nil) dropped: the string literal is the
  key sequence R-E-T, which planted a dead R prefix keymap in every
  minibuffer (live Doom session confirmed polluted - (kbd "RET") still
  exit-minibuffer, so the line never did what its comment claims; the
  cl-letf embark--default-action override is the working part).  Fix
  doom.d-side someday.  embark-preview + embark-ephemeral-act gained
  (require 'embark) - both reachable from a fresh boot before embark
  loads (vertico C-SPC, browser-tab-act).  INHERITED BUG fixed in
  general/autoload/url.el: all three link-bug-reference->* converters
  called embark-target-bug-reference-at-point, which is VOID - the
  defun is ...-link-at-point (doom.d :35 carries the same bug); specs
  pin the fixed path (tests/general/url-tests.el, new).
  link-org->just-text gained its missing autoload cookie
  (embark-org-link-map "c s" binds it).  Macro shape: embark-ace-action
  lost doom's pointless eval-when-compile wrapper; both macros carry
  cookies, loaddefs load before config.el so the :config map! resolves
  them (probe-verified: generated ace/split defuns fboundp after embark
  loads).  reddigg-view-comments / hnreader-comment /
  youtube-sub-extractor rows stay runtime-void (parked packages).
  Test-infra finds: Emacs 31's featurep no longer reads a let-bindable
  `features' - fake features need function stubs (with-fake-feature in
  tests/embark/embark-tests.el); value-less (defvar foo) marks specials
  only file-locally, so suites stubbing package vars that module fns
  LET-bind must use valued defvars (the embark-indicators lesson).
  Verification: 402 specs (+30: tests/embark/ + the new
  tests/general/url-tests.el), smoke 141 -> 143, probe green (deferral,
  startup binds, url-type keymaps + finders, avy ?. glue, no +residue,
  which-key-- internals present on the built-in copy, quarantine, no
  stray minibuffer R prefix); *Warnings* on consult load shows
  pre-existing consult-customize noise for two unported search fns
  (search-module scope).
- [2026-07] writing module ported (jinx, sdcv, mw-thesaurus, define-it,
  separedit, youtube-sub-extractor, google-translate fork
  :branch improvements + google-translate-posframe riding its build
  (:ensure nil, posframe is a soft require - no build-order hazard),
  occult, prisma, wiktionary-bro, spacehammer with :files ("*.el") -
  installed on every OS for smoke determinism where doom.d gated darwin;
  only Hammerspoon ever invokes it).  local-dev-packages +5:
  google-translate/occult/prisma (existing checkouts, clean + pushed),
  wiktionary-bro extracted, spacehammer -> ~/.hammerspoon (see Local
  :repo packages).  abbrev_defs asset tracked in-module and written in
  place by abbrev - sanctioned exception #2, AGENTS.md updated (user
  verdict).  Deviations: google-translate deferred - doom.d demand-loaded
  it at startup via :init require; the module autoload file requires it
  instead (its transient suffixes read package defcustoms), and loading
  it chains google-translate-posframe via :after, so the transient's
  posframe-mode suffix resolves by the time the layout renders.  jinx's
  after! vertico-multiform became after! vertico + explicit require +
  enable: ground-truthed (live eval + rg over both trees) that this
  writing block is the ONLY multiform enabler config-wide - in the lab
  nothing else ever loads the extension, so the doom.d shape would never
  fire.  number-to-words npm install quarantined to
  doom-cache-dir/number-to-words/ (doom.d ran a bare npm install in
  whatever cwd was current - that is how autoload/node_modules got
  there).  doom.d rot found: prisma's :after (markdown org) gate never
  fired - no `markdown' feature exists (live featurep nil; prisma only
  ever loaded through paste-convert-kill's call-time require) - now an
  install-only stub.  Renames (plus-ban/shadowing):
  jinx-autocorrect-last, google-translate-es->en / en->es (embark + root
  config expected these since their ports),
  google-translate-buffer-listen-source / -translation (bare de-plus
  collides with the package's google-translate-listen-translation),
  markdown-insert-stored-link (markdown-insert-link is markdown-mode
  API), define-it--find-buffer -> define-it-pop-to-buffer-a (advice doing
  package-namespace squatting), jinx-mode-off -> jinx-mode-off-h with
  (&rest _) - serves both the wiktionary-bro hook and
  github-topics-prs-buffer-hook (the jinx hook the git port deferred
  here, now registered), spacehammer--hs-eval-fennel ->
  hammerspoon-eval-fennel, spacehammer--frame-facing-direction ->
  frame-facing-direction; the wiktionary hook now points at lab's
  eww-open-in-other-window.  insert-comma relocated home from ai
  (autoload + specs; ai loaddefs regen on mtime).  Drops
  (git-resurrectable): after! writegood-mode (only un-did :checkers
  grammar's hook - package never installed here), after! grip-mode
  (parks with :lang markdown), lsp-marksman require (parks with lsp),
  menu-bar-item-set-clock-or-pomodoro (org-pomodoro long-tail owns its
  consumer hook), translate-at-point-smart (pdf module sole consumer).
  harper-ls after! lsp-mode block ported INERT (registers when lsp
  lands) minus its stray (setq c-basic-offset 4) tail.  Latent org bug
  surfaced by the probe requiring org: setopt org-image-actual-width
  '(0.7) trips org 10.0-pre's stricter defcustom type while
  org-display-inline-image--width still honors fractions - plain setq
  now.  Test-infra lesson pinned: defun inside describe cannot see
  buttercup's lexical :var slots - recorder fns must be closures built
  in before-each.  Verification: 439 specs (+37: six tests/writing/
  suites; insert-comma's 3 moved from tests/ai/), smoke 143 -> 160
  packages 0 failed/blocked (keep-set + popup/showtip/pos-tip and
  friends), probe green (boot deferral incl. post-probe re-check, jinx
  on doom-first-buffer + mode-map binds, multiform activation + jinx
  grid category, yank/paste + occult + paragraph advice registered,
  github-topics hook, abbrev wiring incl. live "dont"->"don't"
  expansion, tkk pin + posframe chain + void-free transient layout
  walk, markdown/org insert-state glue (org "[ SPC" rides vulpea,
  doom.d-parity), separedit startup binds, zero rename residue, no new
  *Warnings*).  Keydiff 88 -> 77 open / 83 broken / 84 resolved /
  0 regressions / 0 lab-only - SPC x cluster off the table (l/t trees
  exact-matched; es->en / en->es rows :improved).  Unblocked: clojure
  (spacehammer-monroe), chat (jinx).
- [2026-07] search module ported (consult-omni develop UNPINNED, tldr,
  slacko LOCAL-REPO; consult-symbol relocated in from the root layer,
  zoxide-find relocated in from shell).  consult-omni: the Feb-2025 pin
  dropped after upstream audit - develop gained consult 3.x
  compatibility and if-let*/when-let* fixes since (35 commits); the
  :files (:defaults "sources/*.el") recipe dissolves doom.d's straight
  load-path hack (org-roam precedent).  Source enabling switched to the
  package API: consult-omni-sources-modules-to-load +
  consult-omni-sources-load-modules in :config replace doom.d's
  hand-rolled require loop (consult-omni-load-sources+ was
  byte-for-byte what the API does) AND the :before advice that
  triggered it - the transient's autoload file requires consult-omni at
  top level instead, so the first SPC s / loads the stack and keydump
  sees no :void-suffix (source files carry no autoload cookies).
  Sources kept: google, brave, wikipedia, browser-history, gptel, gh,
  youtube, apps; dropped: elfeed (package dropped at web-browsing),
  notmuch (returns with its module), duckduckgo/invidious/line-multi
  (loaded in doom.d, surfaced nowhere).  API keys:
  consult-omni--set-api-keys rewritten as four plain setq lambdas over
  general's existing auth-host->pass - the doom.d version pulled in
  dash for a trivial compose, its (set (intern (symbol-name key))) was
  identity rot, and its auth-source-debug let wrapped the BUILD of the
  lambdas, not their calls; function-valued keys are package-sanctioned
  (consult-omni-expand-variable-function).  Scopus key dropped (source
  never loaded).  The no-require-match advice had survived an upstream
  signature change only by plist-put positional accident; reimplemented
  structure-aware as :filter-args (consult-omni-no-require-match-a,
  autoload/, batch-tested) - consult-omni-multi passes :require-match
  nil itself, but gptel/youtube/apps sources declare t, which would
  block the free input their :on-new handlers exist for.  Transient
  rows dropped: elfeed + notmuch (void-suffix class, mpv-transient
  precedent) and search-in-slack - superseded by slacko-search (user
  verdict): slacko searches inside Emacs, the hammerspoon app-driving
  variant retired with its macos gate.  slacko: public repo,
  checkout/doom.d-submodule/origin all in sync at ef754e1; plain github
  recipe + local-dev-packages entry; slacko-creds.el is mechanism-only
  (runtime token extraction, no secrets in-repo).  Quarantine:
  tldr-directory-path + slacko-creds-gpg-file -> cache dir
  (slacko-emoji-cache-directory self-derives from doom-cache-dir).
  process-external-url REWRITTEN over embark-url-config (user verdict;
  the pcase duplicated the embark table coarsely - its blanket github
  branch sent commit/compare urls into forge): now first matching
  url-type's RET action, shared-RET fallback, bug-reference branch
  kept.  Zero external callers found (mxp/hammerspoon/dotfiles greps) -
  its one live consumer is the consult-omni video embark action ("e").
  embark-project-search's consult-ripgrep rebind is FINAL: doom.d's
  +vertico-file-search call shape is exactly that, and search brings no
  file-search fn.  search-other-project +
  search-notes-for-symbol-at-point vendored into bindings/autoload.el
  (project-prompt-project-dir / org-directory) - the two voids behind
  the consult-customize *Warnings* noise flagged at the embark port;
  the probe pins consult now loading warning-free.  doom.d rot fixed on
  port: search-in-project seeded literal "nil" between symbols
  ((symbol-name (symbol-at-point)) on nil) - thing-at-point now,
  spec-pinned; autoload/consult-omni.el carried a stale web-browsing
  header comment.  with-fake-feature promoted from embark-tests.el to
  tests/helper.el (second consumer).  Probe-infra lesson: -l files load
  after after-init-hook, so on a warm cache probes must check
  elpaca-after-init-time and only otherwise hook (smoke-check.el
  already documented it; the hang looked like a block but was
  nobody-calls-kill-emacs).  Verification: 458 specs (+19: two
  tests/search/ suites, misc-tests rewrite, bindings additions), smoke
  160 -> 163 packages, 0 failed/blocked, probe green (deferral, 7 bind
  rows, warning-free consult load, sources + suffixes + advices + key
  fns after transient force-load, embark-table dispatch x3, residue
  sweep, quarantine x3, dired zoxide hook, theme + modeline guards).
  Keydiff 77 -> 74 open / 70 broken / 10 parked / 90 resolved /
  0 regressions - SPC s cluster CLOSED (4 rename rows :improved, 6
  lookup rows + link-hint + vundo parked :deferred), SPC n * and
  SPC f d :improved, SPC * exact-matched.  Post-push amendment: CI
  smoke failed on slacko - its Package-Requires pinned (emacs "30.2")
  while CI runs the 30.1 floor; local 31 masked it (the gptel-tools
  floor lesson repeated, this time caught by CI not foresight).
  Floor lowered to 30.1 upstream (slacko 37c9018; 30.x point releases
  are bugfix-only, the pin was the dev-box version).  Check
  Package-Requires floors against CI's 30.1 at every future own-package
  port.
- [2026-07] tab-bar module ported (registry-only: no packages; slot
  modeline < tab-bar < elisp, doom! order).  Everything lives in the
  LAZY autoload/tab-bar.el (colors precedent - the transients would
  drag built-in transient in at boot from an eager autoload.el); no
  eager file at all.  Renames collision-checked live: built-ins
  tab-bar-rename-tab / tab-bar-duplicate-tab would be shadowed by naive
  de-plussing, their wrappers are tab-bar-rename / tab-bar-duplicate;
  the movers joined the built-in alias family as tab-move-left/right;
  the rest de-plussed straight (tab-bar-kill-tab, tab-bar-add-new-tab,
  tab-bar-name-fn, tab-bar-fmt-show-index-fn, tab-bar-move-buffer-to-tab,
  tab-bar-kill-project-buffers, tab-bar-find-buffer-in-tabs).  Dropped:
  +tab-bar-switch-to-tab-number (dead 'last branch; the transient's
  hidden goto rows call tab-bar-select-tab directly), the namespace-
  squatting defvars tab-bar-tab-added-hook + tab-bar-tab-removed-hook
  (added is run NOWHERE - its tab-bar-template-run-hook-h registration
  was dead code; removed had zero registrations, kill-tab's run-hooks
  was a no-op), the corfu tab-bar-mode-hook (corfu removed config-wide),
  commented reset-frame-full-height + tab-bar-created-h hooks.  doom.d
  rot fixed with specs: rename-dups processed only the FIRST duplicate
  group (-separate returns a two-list partition; the -map + pcase-let
  first-element destructure ignored the rest) - rewritten on
  seq-group-by, the digit-suffix regexp tightened from * to + (the *
  form also ate a bare trailing space); move-buffer-to-tab tested (length
  (window-list)) - always truthy, the delete-window arm was dead (now
  length= 1: sole window buries, others delete); kill-project-buffers'
  (featurep 'project) guard made it a silent no-op (now require);
  run-with-timer "0.3 sec" string -> 0.3.  name-fn rewritten over
  project.el (projectile dropped), branch/worktree detection still
  magit; macos gate faithful: installs only (unless (featurep :system
  'macos)).  tab-bar-history-mode +1 (limit 100) SIDE EFFECT: it arms
  general's window-undo/redo engine - the advice rides the mode's
  pre-command hook, and nothing enabled the mode before this port, so
  SPC w u/r had recorded nothing in the lab until now.  Transient
  updates on copy: zoxide rows -> zoxide-find, gptel+ -> open-gptel,
  doom/switch-to-scratch-buffer -> switch-to-scratch-buffer,
  find-in-doom-dir -> find-in-config-dir, roam template rows call
  open-journal/vulpea-find/vulpea-backlinks directly instead of
  general--simulate-keys (orr's "Backlinks" desc was rot - it lands on
  roam find), projects row rebuilt on project-prompt-project-dir +
  dired; rows dropped: elfeed (package dropped), notmuch/telega
  (return with their modules; jira's "jb" -> go-jira-browse-default-board
  came back at the jira port 2026-07), hnreader/reddigg (disabled forks), "ei"
  emacs.d dired (doom-emacs-dir IS the config dir here).  Hints
  cleanup: named self-removing tab-bar-hints-off-h replaces doom.d's
  anonymous lambda that accumulated on transient-exit-hook and stayed
  resident forever.  Desktop persistence decided deliberately:
  desktop-path quarantined to doom-state-dir (doom-compat.el);
  desktop-save-mode enables on doom-after-init-hook (elpaca-after-init,
  all packages activated); automatic desktop-read NEVER fires (desktop.el's
  own after-init hook registers post-init - same effective behavior as
  doom.d) so restore stays manual (transient "dr" / SPC q l);
  desktop-dirname preset to the same dir - doom.d rot ground-truthed
  live: desktop-save t with nil dirname prompted read-directory-name on
  every real exit and no desktop file was ever written in Doom's state
  dir (stray desktop-dirname .../state/desktop, no .emacs.desktop
  anywhere); the auto-save timer stays inert until the first save/read
  (desktop-owner pid gate).  Modeline follow-up verdict: doom-modeline's
  workspace-name segment stays OUT of `main' - it does render tab names,
  but tab-bar-show t keeps them permanently visible in the tab bar line
  and doom.d's modeline never showed one (note updated in
  modeline/config.el).  Test-infra lesson pinned: real tabs always carry
  the explicit-name cons (tab-bar--tab emits it) - rename-dups' in-place
  setf relies on that; a fixture without the key sees the setf prepend
  to the lambda-local binding and the mutation vanish.  Keydiff: SPC l
  closed (:ported, the gated root row went live); SPC p cluster closed -
  browse-project / browse-in-other-project / find-file-in-other-project
  implemented plus-free over project.el in bindings/autoload.el
  (search-other-project precedent), discover-projects dropped
  (project-remember-projects-under at SPC p a IS discovery), 6
  projectile-only rows dropped (C/I/R/e/g/s), 8 :improved project.el
  rows ledgered; SPC q closed - quicksave-session (new, module
  autoload; also the transient's "ds") at s, restore-desktop-and-tabs
  at l, built-in restart-emacs (29+) at r, F/S/L dropped (kill-all-
  buffers + the named-file session pair; single canonical session dir
  policy), d parked :deferred on the session-switch infra.  73 -> 59
  open / 70 -> 59 broken / 11 parked / 114 resolved / 0 regressions.
  Verification: 485 specs (+27, tests/tab-bar/), smoke 163 packages
  unchanged (0 failed/blocked), probe 26 checks green (config applied,
  binds incl. SPC l, layouts void-free after force-load, live
  rename-dups + hint rendering, desktop wiring + quarantine, no
  +tab-bar residue, no *Warnings*).

- [2026-07] daily-driver fixes, first real-use feedback batch.
  (1) C-h k / SPC h k landed on stock describe-key: Doom CORE remaps
  describe-{key,function,variable,command} to helpful globally, a layer
  the vendored bindings tree never carried - and a fourth audit-blind
  class: command remapping resolves at execution time, both sides dump
  the same describe-* symbols.  Remap set added to the root helpful
  block (:init, bound at startup); describe-function keeps the elisp
  module's helpful-symbol advice for programmatic calls (key presses now
  take the remap to helpful-callable, like live Doom).
  (2) SPC w L on a top/bottom stack errored "No window right from
  selected window": the pilot's windmove-swap-states rewrite kept only
  doom.d's wrap-at-edge override and lost the Doom-core engine the
  override DELEGATED to (+evil--window-swap; doomemacs#7218 has since
  folded the wrap upstream).  Ported the full engine as
  window-move-in-direction: swap-and-follow when a window exists,
  far-throw when only perpendicular neighbors exist (the bug case),
  wrap from a full-span edge window (always on, doom.d-wrapper
  behavior; doom.d never wrapped J/K - lab keeps all four uniform),
  split-and-follow for a sole window (Doom semantics; the old lab spec
  pinned wrap-on-sole-window, an accident of the pre-#7218 guard's
  window-at-side-p check, not intent).  Vacated window shows
  get-scratch-buffer-create (doom-fallback-buffer analogue - routes to
  the persistent *scratch* via the general module's override).
  evil-tests window-move suite rewritten: real-window swap/split specs,
  recorder stubs for the evil far-move/wrap arms, dedicated-window
  user-error pin.
  (3) org-superstar pulled out of the org long-tail (misjudged there:
  heading/todo/item bullets are daily-visible rendering, unlike the
  on-demand verb/anki tail); doom.d block ported verbatim (setopt,
  org-mode hook).  Org audit rode along: insert-dash/bracket-pair
  un-voided by the writing port as planned, org-noter row pruned by its
  +noter guard, no org-hide-leading-stars conflicts; org-cliplink is
  the next-most-visible gap (five fboundp-gated url.el title-retrieval
  sites + web-browsing's org branch + the dropped "i L" localleader) -
  still parked on the long tail, flagged for its own pull-forward call.
- [2026-07] SESSION SWITCH executed (Infra debt entry has the detail):
  the lab is the daily driver now, Doom retired to a text-only porting
  reference (AGENTS.md hard boundaries inverted).  Server plumbing:
  guarded server-start on doom-after-init, default socket - the
  elisp-eval MCP's server.bb runs plain =emacsclient --eval= (no -s
  anywhere, verified), so it and every other emacsclient consumer (mxp,
  Hammerspoon) work unchanged; restart-server ported (renamed
  +default/restart-server), SPC q d ledger :deferred -> :improved.
  Rode along: triage of the user-reported elpaca failures
  (elpaca-use-package "Condition (finished . elpaca) failed", occult +
  sdcv "Subprocess error", 95s/142s) - UPDATE-CYCLE failures, not boot
  failures: all three builds exist on disk and the day's smoke runs were
  0 failed/blocked.  elpaca-use-package builds out of the elpaca repo's
  extensions/ (its failure is inherited from the =elpaca= order's own
  fetch/rebuild), occult fetches over SSH (agent-less subprocess timeout
  shape), sdcv clones from repo.or.cz (notoriously flaky, [tree:0]
  filter).  Root-causing needs that session's *elpaca-log* - deferred to
  the first lab-hosted sitting, which should re-run the cycle live; this
  doubles as the still-open "one elpaca-manager update cycle" trial
  gate.
- [2026-07] CI smoke segfault root-caused and BYPASSED.  456a71b's smoke
  died with "Fatal error 11" on both runs (deterministic; rerun same sha,
  same byte-identical backtrace) while the batch test job passed.
  Forensics: same nix emacs store path in passing and failing runs (env
  drift excluded), no libgccjit in the closure (no native comp - eln
  theories dead), addr2line against the exact CI binary (pulled from
  emacs-ci.cachix into a throwaway nix container) resolved the crash to
  fill_up_frame_row_with_spaces <- build_frame_matrix_from_window_tree <-
  update_frame: Emacs 30.1's TTY frame-glyph-matrix padding - an upstream
  C bug, not config logic.  Trigger matrix: install-heavy runs passed
  (writing port), machinery-only runs passed (tab-bar port, zero new
  packages), machinery + install-during-boot crashed at first redisplay.
  Container repro fizzled (cold-boot network timeouts on arm64; a
  compat-rooted dependency cascade under Rosetta x86) - abandoned per
  cost/benefit.  Bypass, two-pronged: scripts/smoke-check.el sets
  inhibit-redisplay for the whole CI pty boot (rendering was never the
  probe's signal; markers are file-based) and the modeline anti-flash
  blanking is now GUI-gated (a tty got mode-line-less windows handed to
  exactly the crashing matrix code).  Floor policy unchanged: CI stays on
  latest stable (30.1); local 31 is the dev-box exception.  Rode along:
  fringe-mode call in position-frame-on-load-h fboundp-guarded (void on
  fringe-less nox builds - the CI emacs; the "void fringe-mode" boot
  message was pre-existing noise there).
- [2026-07] elpaca update cycle completed live (first lab-hosted sitting) -
  the trial gate closes.  All three reported failures root-caused via the
  event log (this elpaca has an EVENT SYSTEM: per-order logs live in
  =elpaca--event-log= as elpaca-event structs; the old =elpaca<-log= slot
  is gone - query events by id, statuses still via =elpaca<-status=).
  (1) occult: NOT the suspected ssh-agent env - the ssh fetch succeeds in
  the GUI session (launchd SSH_AUTH_SOCK + keychain key resolution work);
  the real failure was =elpaca--log-updates= running =git log ..@{u}= in
  the ~/GitHub/agzam/occult.el checkout whose =main= had NO upstream
  tracking configured ("fatal: no upstream configured for branch 'main'").
  Fixed with =git branch --set-upstream-to=origin/main main=; audited all
  13 local-dev checkouts - occult was the only one missing tracking.
  (2) elpaca itself: =git merge --ff-only= died with "refusing to merge
  unrelated histories" - the bootstrap installer clones elpaca with
  =:depth 1=, and every subsequent fetch on the shallow repo lands
  disconnected shallow islands (.git/shallow had accumulated ~47 grafts;
  no merge-base with @{u}).  Fixed with a one-time =git fetch --unshallow=;
  ff merges work from now on.  Watch for the same shape in any other
  :depth-pinned repo that later needs updates.
  (3) elpaca-use-package: pure inheritance - builds out of the elpaca
  repo's extensions/, so its "Condition (finished . elpaca) failed" clears
  once elpaca merges.  (Also learned: never queue two elpaca-merge calls
  for orders sharing a source dir - concurrent git in one repo trips
  index.lock; elpaca-merge-all's shared-dir blocking exists for this.)
  sdcv (repo.or.cz, treeless filter) fetched clean this time - flaky host,
  no action; treeless clones keep full commit history so their merges are
  not at risk from the shallow trap.  Cycle result: fetch all 164, review
  (2 upstream elpaca commits), merge - 164/164 finished, 0 failed.
- [2026-07] global-key sweep (the M-DEL report): a FIFTH audit-blind
  class - the key audit dumps leader + evil prefix trees only, so global
  modifier keys never entered keydiff (the helpful remaps were the same
  blindness).  One-shot matrix diff (global/insert/minibuffer/vertico
  maps x M-/C-/C-M-/s- keys) between the live Doom session and a lab pty
  dump; 26 rows, adjudicated: Doom config/default text-manipulation rows
  the vendoring never carried are now ported plus-free into
  bindings/autoload.el - backward-to-bol-or-indent /
  forward-to-last-non-comment-or-eol (C-a/C-e/s-left/s-right, org keeps
  org-aware remaps; eshell remaps skipped - eshell-bol obsolete 30.1),
  backward-kill-to-bol-and-indent (s-backspace), newline-below/above
  (C-RET/C-S-RET/s-RET), comment-current-line (s-/, over doom's
  anonymous lambda), explicit M-<backspace> backward-kill-word (the
  reported key; function-key translation to M-DEL isn't reliable),
  :i C-g evil-escape, minibuffer C-/ consult-history, mac s-c/s-w/s-x
  parity binds, s-f on consult-line (:improved - doom.d's swiper row is
  ivy-era bound-but-void rot).  Doom rot fixed with a spec:
  forward-to-last-non-comment-or-eol stored its bounce-back position in
  the BACKWARD mover's variable, so returning from eol never worked.
  Deviation: comment detection in the bol/eot helper uses plain
  (nth 4 (syntax-ppss)) - doom-point-in-comment-p's 40 lines of
  comment-opener edge cases stay unvendored.  Deliberate stands, no
  action: M-r eval-whole-buffer placeholder, insert C-/
  completion-at-point (complete-in-minibuffer died with corfu),
  stock abort-minibuffers on C-g.  False negatives understood: the
  C-RET-family dump rows miss because (kbd "C-RET") is the modified-char
  form [67108877] while GUI/kkp produce [C-return] (Doom hits both via
  its C-m translation layer); insert-map M-<backspace> resolves through
  the global bind at runtime.
- [2026-07] transient inconsistencies swept - sexp-transient ported, two
  expreg-transient regressions healed, one latent mpv bug fixed.
  sexp-transient (SPC k) was bound but void (its lib never loaded);
  restored as a LAZY general-module autoload
  (general/autoload/sexp-transient.el, mirrors expreg.el) instead of the
  planned eager =(load! "lisp/sexp-transient")= - keeps avy + edit-indirect
  out of startup, matching the loaddefs-lazy contract.  The shared bypass
  engine (transient-layout-keys + transient-bypass-keys) moved out of
  expreg.el into lisp/transient-bypass.el - "don't duplicate" honored
  without an eager lib.  Renames/drops: sp-eval-current-sexp ->
  eval-current-sexp (doom.d user fn, renamed at the elisp port); dead
  transient--all-layout-keys gone.  expreg fixes: Markdown section restored
  (writing module now supplies markdown-wrap-code-generic/-collapsible +
  stock markdown-insert-*), bypass "%" back to evilmi-jump-items
  (evil-matchit ported) over the stop-gap evil-jump-item; Magit stays
  dropped (bypass "s"/"x" collision).  Latent bug: mpv-transient (C-x m)
  called transient-bypass-keys with no require - leaned on expreg.el
  loading first, errored at open otherwise; now (require 'transient-bypass)
  in mpv.el (a THIRD user the engine move surfaced).  Tests: new
  tests/lisp/transient-bypass-tests.el (fixture engine specs) +
  tests/general/sexp-transient-tests.el (prefix + command rot-net + the
  eval-rename guard); expreg rot-nets extended for Markdown; mpv gains a
  bypass-engine-available spec.  bb test 565/0; live setup-children probe on
  expreg/sexp/mpv prefixes all clean (the doom.d "errored at open" class).
  SPC k closed, SPC v note updated.
  Keydiff follow-up (2026-07, lsp/clojure review): the bare keydump boot
  flagged SPC k + SPC v :void-suffix (1 regression) - the restored
  :if-gated Markdown/Clojure sections reference package commands
  (markdown-insert-*, clojure-toggle-ignore, lsp-clojure-thread-*) that
  aren't autoloaded, so they read void until their feature loads.  The live
  setup-children probe missed it (packages already loaded there).  Fixed the
  keydump-config, not the transients: KEYDUMP_REQUIRE gained markdown-mode,
  clojure-mode, cider, lsp-clojure (the org/vertico false-void precedent).
  Both rows drop off the diff as exact matches; keydiff back to 0
  regressions.
- [2026-07] yaml module ported (registry slot after writing, before the
  darwin osx tail; (:custom yaml)).  Packages: yaml-mode, jinja2-mode,
  highlight-indent-guides, yaml-pro (GitHub pin, like doom.d).  Picked as
  the next module for being fully self-contained: no new stub infra (unlike
  golang/nix, which want set-repl-handler! from :tools eval) and no cross-
  module entanglement (unlike lua's friar/fennel-ls + the clojure module's
  inert fennel map!).  It also closes a designed integration point - the
  tree-sitter module already ensures the yaml grammar lazily on yaml-pro
  load (with-eval-after-load 'yaml-pro treesit-ensure-installed 'yaml; that
  treesit block is 31-gated, CI's 30.1 skips it, grammar present locally).
  Deltas from doom.d: doom-theme is undefined here (doom-defaults dropped
  it), so highlight-indent-guides' initial face refresh guards on
  custom-enabled-themes instead - a theme is enabled by the first yaml-mode
  open (the colors module's own ring anchor); doom-load-theme-hook already
  exists and carries the ongoing refresh.  The two hook fns extracted to
  modules/yaml/autoload/highlight-indent-guides.el for the batch tier
  (tree-sitter fold precedent) and de-plussed: +indent-guides-init-faces-h
  -> indent-guides-init-faces-h, +indent-guides-disable-maybe-h ->
  indent-guides-disable-maybe-h; both added from highlight-indent-guides'
  :config (not module top level) so the theme refresh only fires once the
  package - and thus highlight-indent-guides-auto-set-faces - is loaded.
  highlight-indent-guides-method/-suppress-auto-error stay setq in :init
  (custom types undefined pre-load); jinja2-enable-indent-on-save is setopt
  in :config (loaded defcustom).  yaml-pro-ts-mode-map keeps doom.d's binds
  verbatim ([remap imenu] -> yaml-pro-jump, C-c C-f unbound, evil :n subtree
  nav/fold/move).  Verification: 571 specs (+6, tests/yaml/highlight-indent-
  guides-tests.el: display-graphic-p face gate + org-indent disable logic),
  bb lint 186/0, smoke 195 packages 0 failed/blocked no new *Warnings*, live
  probe 30/30 (yaml-mode + yaml-pro-ts-mode + highlight-indent-guides active
  on a .yaml buffer, grammar ready, plain + evil-normal binds resolve to real
  commands, both hook fns wired onto doom-load-theme-hook / org-mode-local-
  vars-hook, jinja2 .jinja + indent-on-save nil).  Keydiff untouched: all
  binds are yaml-pro-ts-mode-map-local, not leader/global.
- [2026-07] python + lua modules ported together (frequent daily drivers,
  user request; registry slots after clojure - both call lsp!, the lsp
  lookup handlers, and clojure's fennel monroe glue).  Currency review
  (2026): python's basedpyright + ruff + uv toolchain stands - lsp-pyright's
  basedpyright API is unchanged (lsp-pyright-langserver-command
  "basedpyright"), ruff's modern LSP is =ruff server=, uv is standard; the
  Rust checkers (pyrefly 1.0 2026-05, ty 0.0.x beta) noted as emerging
  options, basedpyright kept as the mature default.  BIGGEST find: lua-mode
  is BUILT-IN as of Emacs 31 - immerrr's mode was upstreamed into
  progmodes/lua-mode.el (FSF 2025-2026), carrying lua-indent-level and
  lua-search-documentation; .lua maps to it with no lua-ts-mode remap.  So
  :ensure nil, the MELPA lua-mode package dropped (lua-ts-mode is also
  built-in; lua-mode kept per the user's config).  friar DROPPED: it drove
  an AwesomeWM (Linux/X11 WM) fennel REPL off a /home/ag/.luarocks path -
  useless on this macOS box, where the Lua/Fennel work is Hammerspoon, owned
  by spacehammer (writing) + the clojure module's monroe glue.  Nothing
  bound friar/awesomewm-repl (doom.d's "awesomewm config" dired shortcut is
  a separate Linux-only bind, not ported); package + defalias +
  friar-fennel-file-path setq all gone.  fennel-mode kept (MELPA, no
  built-in): fennel-ls lsp client registered by hand (lsp-mode ships no
  fennel client), the imenu->consult-imenu remap kept (fennel-ls has no
  textDocument/documentSymbol), lookup handlers + darwin Hammerspoon docset;
  lsp start via lsp! (deferred, guarded) over doom.d's bare lsp.  This
  closes the clojure entry's "fennel map! waits on lua" deferral - the inert
  :after fennel-mode monroe keymap now arms.  python renames (plus-ban):
  +python-mode-lookup-handlers -> python-lookup-handlers-h (rewired to the
  lab's lsp-lookup-definition-handler / lsp-lookup-references-handler /
  consult-dash-doc), py-fully-qualified-symbol-at-point ->
  python-fully-qualified-symbol-at-point (py- squats the python-mode.el
  package namespace).  Dash-free: python--fqn-via-lsp's -some-> hover chain
  rewritten with when-let* (the module's only dash use).  Deviations from
  doom.d, all lab-robustness: the localleader map! and the ruff
  python-fix-imports :override advice moved into the =python= block so they
  arm on the first .py buffer instead of lsp-pyright's deferred load;
  lsp-ruff-python-path set via =after! lsp-ruff= not doom.d's combined
  setopt over an unloaded defcustom; the two duplicate ("f" . "format")
  localleader prefixes merged.  doom.d rot fixed: antifennel-install's shell
  string ended in a dangling "&&" (its cleanup rm was commented out) with a
  mismatched 5th format arg - it would fail at the shell; dropped the
  trailing && + extra arg (fnlfmt/fennel-ls installers were already right).
  Ground-truthed as real (not rot), kept: magit-python-which-function +
  magit-log-trace-definition-function (fboundp in current magit),
  edit-indirect (transitive via separedit), the built-in python-fix-imports
  (Emacs 30+, the advice target).  Verification: 599 specs (+18: tests/lua/
  x9 incl. the antifennel trailing-&& regression + friar-drop guard,
  tests/python/ x9 - path-to-dotted / resolve-project-module __init__ walk /
  uri-to-module regexes / relative-import resolution / ruff command /
  rename guards), bb lint 195/0, smoke 191 -> 198 packages (fennel-mode +
  lsp-pyright + deps; lua-mode built-in adds none) 0 failed/blocked, live
  autoload sanity green (renames resolve, +python-/py- names gone,
  friar/awesomewm-repl unbound, python--path-to-dotted round-trips).
- [2026-07] pdf module ported (registry slot after yaml, trailing the common
  list; org-noter defers on org and nov rides writing's translate/jinx
  autoloads, both loaded ahead).  HEADLINE - continuous scroll is UPSTREAM
  now: vedang/pdf-tools ships lisp/pdf-roll.el (autoloaded
  pdf-view-roll-minor-mode, lighter " Continuous") as of v1.2.0 (2025-12) /
  v1.3.0 (2026-01), so the dalanicolai fork + its pdf-roll branch AND the
  separate image-roll package are BOTH dropped - the roll code was absorbed
  into pdf-roll.el, which only requires pdf-view (0 image-roll refs in the
  repo).  Recipe tracks vedang's default branch; :files keeps
  build/{Makefile,server} so epdfinfo still compiles at first PDF open.  The
  fork-era <wheel-up>/<wheel-down> remaps in pdf-view-roll-minor-mode-map are
  dropped: the upstream mode drives the wheel via mwheel-scroll-{up,down}-
  function.  pdf-view-continuous (the "s c" toggle) is a distinct,
  long-standing per-line variable and survives untouched, so both scroll
  features remain.  epdfinfo builds at RUNTIME (pdf-tools-install-noverify +
  the graceful pdf--install-epdfinfo-a advice), never at elpaca build time -
  CI smoke never opens a PDF, and the binary lands in the package build dir
  under .local (self-contained, no quarantine).  Kept: saveplace-pdf-view
  (reopen at last page), org-noter (settings + the 3 window/scroll advices),
  nov (epub).  Dropped: calibredb (unused, user call), image-roll (obsolete).
  Added: hide-mode-line - Doom pulled it via :ui, the lab had no consumer
  until the annotation-list mode-line.  Cross-module resolutions:
  translate-at-point-smart un-parked into writing/autoload/google-translate.el
  (its only consumer is nov's localleader "T"; the deferred
  google-translate-posframe file is pulled in via require at call time -
  doom.d loaded google-translate eagerly); org-mode-map localleader "n" ->
  org-noter-transient restored (doom.d org config.el:80, the "waits for pdf"
  deferral) and pdf-view-mode-map localleader "n" too; SPC n e -> org-noter
  filled from the pdf module - Doom's row is guarded by
  (modulep! :lang org +noter), structurally false in the lab (org is :custom),
  so the owning module fills the pruned slot (SPC n r roam-tree precedent).
  Renames (plus-affix sweep + when-let*): org-noter-anchor-to-current-page+ ->
  -page, +pdf--install-epdfinfo-a -> pdf--install-epdfinfo-a, the org-noter
  scroll helpers' when-let -> when-let*.  doom.d rot fixed: nov-text-width was
  set to 100 then clobbered with t in one setopt - kept t (the effective
  value, fill-the-window with visual-line-mode).  Verification: 625 specs
  (+26: tests/pdf/{pdf,nov,org-noter} + 2 tests/writing translate-at-point-
  smart; packages provide-stubbed, cl-letf spies, org-noter-transient suffix
  walk), bb lint 202/0, smoke 198 -> 205 packages (pdf-tools + tablist +
  saveplace-pdf-view + org-noter + nov + hide-mode-line) 0 failed/blocked,
  probe PROBE-OK (25 checks: pdf-roll shipped + roll mode autoloaded, every
  module command fboundp, .pdf/.epub/%PDF wiring, SPC n e + org/pdf
  localleader resolved via the evil aux keymap with a "," "." control, no new
  *Warnings*).

* Key clusters
The key audit kills the "binding X silently missing" whack-a-mole:
mechanical keymap dumps diffed per cluster, every differing entry gets an
explicit verdict in key-decisions.edn (:ported / :improved / :dropped /
:deferred + note). Migration-scoped tooling - retire when the last cluster
closes (teardown TODO below).

Workflow, one cluster per sitting - any future session (human or LLM)
resumes from this section alone:
1. Regenerate BOTH dumps at the start of a sitting; dumps are throwaway
   snapshots, never trust old ones.
   - Doom side (post session-switch flow, ACTIVE since 2026-07 - no live
     Doom session exists to eval in): dump via boot mode against the
     retired Doom install -
     =KEYDUMP_OUT=~/.cache/emacs-lab/doom-keys.eld TERM=xterm-256color
     script -q /dev/null emacs -nw --init-directory ~/.emacs.d -l
     ~/.config/emacs-lab/scripts/dump-bindings.el= (the script is
     standalone vanilla elisp, safe in a throwaway Doom boot; boot mode
     force-loads autoloads, so doom-side :void-suffix detection gets
     sharper - keydiff still only reads lab flags).  Untested flow:
     sanity-check the entry count (~624, the last live-session dump) on
     first use.  Historical flow while Doom still ran: eval the walker
     half (above the "Boot mode" section) inline in the live session,
     dump-bindings-load-autoloads nil - the one sanctioned lab-code-in-
     Doom exception, retired with the switch.
   - Lab side: =bb keydump= (pty boot; KEYDUMP_REQUIRE env forces
     features whose maps bind lazily).
2. =bb keydiff= - org report at ~/.cache/emacs-lab/keydiff.org (generated,
   never hand-edited), console shows open/broken/parked/regression counts
   per cluster; exit 1 on regressions (a :ported entry that drifted).
   Broken = bound to a void command, or a transient prefix whose layout
   references void suffix commands (lab dump marks those :void /
   :void-suffix); only a :deferred verdict parks one, other verdicts
   can't silence it.
3. Pick an unchecked cluster below. For each open entry record a verdict
   in key-decisions.edn and port/improve as decided - the binding itself
   is what closes a diff entry; a :ported ledger line is optional
   bookkeeping that buys drift detection. Rerun 1-2 until the cluster
   shows 0 open, tick it here, put session-worthy calls in the
   Decisions log.

Module-port rule: when a module lands, flip its :deferred ledger entries
and re-review its cluster. Coverage: leader tree + evil g/z/[/] prefix
trees; localleader and mode-local maps get scoped dumps later if needed.

Cluster checklist (open counts as of 2026-07 pilot; =bb keydiff= is truth):
- [X] SPC w :: pilot - 12 verdicts; ace-window keys went live with the
  dired port (W/D/M :ported, C-S-w/C-c :dropped), golden-ratio landed with
  the elisp sitting - cluster fully closed, off the diff table
- [X] SPC h :: closed - help-map surgery vendored into bindings/config.el
  (b subtree on built-in which-key, describe-* drift matched, T on the
  renamed toggle-profiler); h p d broken fix (stock describe-package);
  stock d/P/C-h kept over Doom's rebinds, woman over man-or-woman; the
  3 lab-only rows verdicted or structural - off the diff table
- [X] SPC p :: closed with the tab-bar port - browse/find-file-in-other-
  project trio implemented over project.el, discover-projects + 6
  projectile-only rows dropped, 8 project.el rows :improved
- [ ] normal g (12 open) - gd/gD/gI/gf lookup family :improved with the
  lsp/clojure sitting; the rest is evil-mc (g z*), evil-numbers/lion,
  evil-nerd-commenter, :tools eval (g R/r) - waits on those modules
- [ ] SPC x (18) - gptel tree closed with the ai port; the 10 left are
  writing-module material (SPC x l / x t trees etc.)
- [X] SPC c :: code/lsp rows closed with the lsp/clojure sitting - lookup
  five (c d/D/i/k/t) + c W :improved (plus-ban renames), c l on the
  lsp-command-map dispatcher, c j/J un-guarded consult-lsp-symbols
  (vertico guard dropped, org-port precedent); the 5 still-broken rows
  (c e/E/f/s/x) are :tools eval + :checkers, parked with those modules
- [ ] SPC o (15) - open/apps; roam/org tree resolved with the org port,
  o c g closed with the ai port, o r C-b + o e settled with the
  web-browsing port - the rest waits on macos/chat/eval/notmuch
- [X] SPC s :: closed with the search module port - 4 rename rows
  :improved, the lookup six + link-hint + vundo parked :deferred;
  SPC * exact-matched, SPC f d + SPC n * :improved on the way
- [ ] SPC b (8) - buffers; browser rows closed with the web-browsing
  port, 2 left (consult-projectile row, s-d rename verdict)
- [ ] motion [ / motion ] (9+9), normal [ / normal ] (5+5)
- [X] SPC t :: closed - 8 verdicts; flycheck parked on :checkers
- [X] SPC g :: closed with the git module port - 4 verdicts;
  git-timemachine parked on the :emacs vc fold-in decision
- [X] SPC r :: closed with the org module port - r r on vulpea-find, the
  vertico-repeat pair was false-open (KEYDUMP_REQUIRE gained vertico),
  r c corfu leftover dropped
- [ ] SPC f (6), SPC n (6), normal z (3)
- [X] SPC T :: closed with the colors module port - 2 verdicts (the
  renamed cycle-themes pair)
- [ ] singletons: SPC i (a closed with the embark port; l + q closed
  with the tab-bar port - q d parked on the session switch; " and '
  closed with the shell module port)
** TODO Retire key-audit tooling (last cluster closed)
Fold ledger verdicts into the Decisions log; delete scripts/dump-bindings.el,
scripts/keydiff.clj, key-decisions.edn (+ its .gitignore whitelist line),
the keydump/keydiff bb tasks, and tests/scripts/.
Proximity (post tab-bar): open clusters left - SPC b/c/f/n/o/x, the
g/z/[/] trees, and SPC i (the last singleton).

* Testing & CI
- =bb test= - buttercup suites from tests/ (buttercup auto-installed into
  ~/.cache/emacs-lab/test-elpa); =bb lint= - check-parens over tracked
  elisp; =bb smoke= - full elpaca boot behind a pty, verdict from the
  marker file scripts/smoke-check.el writes (SMOKE-OK/FAILED/TIMEOUT);
  =bb ci= - deps + lint + test.
- Workflow (.github/workflows/ci.yml): =test= job (bb ci, cached
  test-elpa) and =smoke= job (cached ~/.local/share/emacs-lab + eln with a
  restore-keys prefix, since elpaca is unpinned). Emacs 30.1 is the floor
  (built-in which-key, completion-preview); local dev may run newer
  (31 pretest as of 2026-07).
- RULE for every future port: the module lands together with a new or
  extended suite in tests/. Layout mirrors the source tree -
  tests/MODULE/FILE-tests.el per module source file (autoload/ level
  flattened; lisp/ sources under tests/lisp/), shared tests/helper.el found
  via locate-dominating-file. helper.el sandboxes XDG dirs, so the
  quarantine wiring itself is testable.
- Coverage map (batch tier):
  - covered :: lisp/doom-compat.el (dirs/quarantine, modulep!, after!,
    hook/advice macros, doom-glob, letf!, quiet!, switch-frame machinery),
    lisp/doom-defaults.el (backup/autosave quarantine, quiet-UX +
    formatting defaults, doom/escape, local-vars hooks, C-i translation,
    built-in lifecycle staging, doom-auto-revert wiring, so-long predicate,
    savehist settings), lisp/functions.el,
    bindings/autoload.el, general (misc, buffers, yank-path, files,
    window movers in evil.el, expreg guards + transient, frames zoom lib
    (geometry stubbed), windows.el layout engine + cleanup), completion
    (completion-preview pager, vertico orderless dispatchers, yasnippet
    helpers), git (magit branch-friendly-string pinned against the dash
    original + python-which-function, misc parse-git-url/bisect-github-url
    ground-truthed against live doom.d, git-link url->ssh + vc-git-link
    homepage wrappers over stubbed browse-at-remote, bug-reference regexp +
    url-format), org (against BUILT-IN org, not the elpaca build:
    insert-item/folds/table-previous-row/babel-results/todo-keywords,
    get-gh-item-title + description-fn with stubbed ghub pinning the
    dash/s rewrites and the repo-regex fix, drawer lint, journal template
    + buffer-type detection, tiff->png advice, goto-bottommost-heading's
    currlevel-leak fix)
  - lookup/lsp/clojure (2026-07) :: lookup dispatch + set-lookup-handlers!
    + backends + online-provider (tests/lookup/); lsp booster json/command
    advice + lsp! + lookup handlers + command-map dispatch (tests/lsp/);
    cider fqn/completion/kill-buffers + polylith root, clojure str-delims/
    unalign/wrap-rich-comment/imenu, monroe reconnect, tools alias/tree/
    storm (tests/clojure/) - all with the package provide-stubbed or
    cl-letf'd.  Batch-tier note: lookup--run-handlers' marker path lives
    in the probe (its set-window-configuration aborts buttercup's runner).
  - smoke-only, and why :: evil module config and
    evil-ex-visual-star-search-a (need evil); expreg--line
    (needs a real display - batch frames are ~10 columns, visual-line fns
    wrap); lisp.el (smartparens pending); url.el (loads now that ghub is
    in, commands still error on let-plist/dash/org-cliplink/embark);
    dash-docs.el (package + network); bindings/config.el and
    lisp/doom-keybinds.el (need general at load time); git module config.el
    plus gh-notify.el/code-review.el autoloads (bodies are pure
    magit/forge/gh-notify plumbing - the live magit-status probe and the
    keydump :void-suffix walk cover them); org module config.el and the
    vulpea/roam-bound autoloads (vulpea-backlinks/forward-links, count
    overlays, org-roam-refile-to-node, vulpea-db-verify, open-journal,
    org-dwim-at-point, yank-from-clipboard - need live vulpea db/structs
    or a GUI; the journal-note live probe walks the whole chain end to
    end)
  - tier-2 idea (not built) :: after =bb smoke= warms the elpaca dirs, a
    package-aware suite could load real evil/consult and cover the
    smoke-only bucket; revisit if that bucket grows
- Gotchas: buttercup-run-discover silently finds nothing from a foreign
  cwd even with an absolute dir arg (the bb test task pins :dir); under
  =bb --config=, =*file*= is cwd-relative - derive paths from the
  =babashka.config= system property; in test files, let-binding a
  package's defcustom that isn't loaded needs a bare (defvar var)
  declaration first or the binding is lexical; deleting an autoload/*.el
  leaves stale generated loaddefs (also rm
  ~/.cache/emacs-lab/autoloads/NAME.el); general/autoload/url.el loads now
  that the git module ships ghub, but its commands still error at runtime
  on let-plist/dash/org-cliplink/embark - stays deferred until those land.
- The smoke job doubles as the Linux-parity trial gate - keep it green.

* Emacs 31 watchlist
Local dev is already on a 31 pretest; when 31 becomes the floor:
- grep-edit-mode may replace wgrep
- igc (MPS GC) may replace gcmh
- tty child frames change the terminal-popup story (corfu-terminal/popon
  are already gone with corfu)
- add a snapshot leg to CI before bumping the floor

* Conventions debt
** DONE Rename sweep: drop Doom =+= prefixes
Done across the whole config (defs + refs, vendored tree included).
Rule for every future port: strip leading =+=, =+module/fn= becomes
=module-fn=; check the target name against the live session for collisions
before settling. Sources in ~/.doom.d still carry =+= names - rename on copy
to match references already renamed here. Non-mechanical mappings already
committed to:
- =+default/yank-pop= -> =yank-pop+=, =+default/dired= -> =dired-prompt=,
  =+default--search-dir= -> =search-in-dir=, =+default/*= -> bare names
- =+eval/buffer= -> =eval-buffer+= (real =eval-buffer= exists)
- =+evil{/,-}window-move-*= -> =window-move-*=
- =+multiple-cursors/evil-mc-*= -> =mc-*=
- =+vertico/switch-workspace-buffer= -> =switch-workspace-buffer=
- =+default/lsp-command-map= -> =lsp-command-map-dispatch= (loads lsp-mode
  then set-transient-map's the real =lsp-command-map=, a defvar keymap with
  no function cell - amended at the lsp port from the earlier "-> real
  lsp-command-map" plan, which can't be a keybinding target),
  =+default/insert-file-path= -> their =insert-file-path= (deliberate merge)
- =doom/delete-backward-word= -> =delete-backward-word=
- =doom-scratch-buffer= -> =scratch-buffer-create= (built-in
  =scratch-buffer= collision); =doom/*scratch-buffer= commands -> bare names
- module flags (=+icons=, =+everywhere=, ...) are modulep! syntax - keep
** TODO Extend file quarantine as modules port
New state-writing packages get their path vars redirected in
doom-compat.el quarantine section (telega, ...).
Done so far: org-roam/vulpea dbs, treemacs persist pair, desktop-path
(tab-bar port; elfeed db moot - package dropped), lsp session +
server-install-dir + eslint-choices, dap breakpoints + extension path
(lsp module port).

* Infra debt
** DONE Relocation to ~/.emacs.d (cord cut)
Executed 2026-07 (user call: backed up ~/.doom.d, moved config to
~/.emacs.d).  The four doom-*-dir defvars (early-init.el + the doom-compat.el
mirror) now derive from =user-emacs-directory/.local/= instead of
=XDG + "emacs-lab"=, so the config is location-independent and all machine
state consolidates under one git-ignored tree:
=~/.emacs.d/.local/{elpaca,elpa,.cache,etc,state}=.  doom-local-dir ->
=.local/=, doom-cache-dir -> =.local/.cache/=, doom-data-dir -> =.local/etc/=,
doom-state-dir -> =.local/state/=.  Test sandbox flipped with it: helper.el
binds user-emacs-directory (was the XDG setenv trio), and the quarantine
self-check now asserts state lives UNDER doom-local-dir (was "outside
user-emacs-directory" - the move inverted it).  Tooling repointed: bb.edn
test-elpa + keydump out, keydiff.clj cache-dir, ci.yml caches -> the new
.local paths.  Old scattered dirs (~/.cache/emacs-lab, ~/.local/share/
emacs-lab, ~/.local/state/emacs-lab) deleted after the first ~/.emacs.d boot
reinstalled elpaca fresh.  .gitignore needed no change - the whitelist
already ignores the new .local/.
** DONE Session switch (daily driver -> lab)
Executed 2026-07 (user call: config in good shape, port largely
functional; the retired Doom instance won't run - its config stays a
text-only reference).
1. [X] server autostart: guarded server-start (server-running-p) on
   doom-after-init-hook, DEFAULT socket - the Doom session ran the same
   shape (non-daemon, "server", ground-truthed live before it retired).
   restart-server ported alongside (renamed +default/restart-server,
   bindings/autoload.el) - SPC q d un-parked.
2. [X] elisp-eval MCP: NO repoint needed - server.bb drives plain
   =emacsclient --eval= (no -s anywhere), default socket just works.
   Same for the other emacsclient consumers (mxp, Hammerspoon).
3. [X] safety inversion: AGENTS.md hard boundaries flipped - live-session
   protections guard THIS config; ~/.doom.d + ~/.emacs.d are read-only
   reference, never loaded into this session.
4. [X] doom-side keydumps: boot mode is the active flow now (Key
   clusters step 1); sanity-check ~624 entries on first use.
One-time costs already wired: Claude OAuth re-auth (cache under
doom-cache-dir) and the eca server download (doom-data-dir).
First-lab-sitting checklist: quit the Doom instance BEFORE the first lab
boot (it holds the default socket; the guard would skip server-start),
verify an emacsclient round trip, then debug the elpaca update-cycle
failures live (see the elpaca-update trial gate note).
** TODO Undo: persistence (undo-fu-session), maybe vundo
Built-in =undo-redo= already covers evil (=evil-undo-system=); plain
undo-fu is redundant on modern Emacs. The remaining value of Doom's
:emacs undo is undo-fu-session (persistent undo across sessions) - port
that (recipes were in general/packages.el, emacsmirror), consider vundo
for tree visualization. The =after! undo-fu= block in config.el goes away
if undo-fu itself never lands.
** DONE Smartparens base config
Landed with the elisp sitting (see Decisions log): core vendored into
general/config.el, the inert sp-pair block now active. sexp-transient
restored 2026-07 (lazy general-module autoload; its own DONE entry below).
** DONE Restore sexp-transient (as a lazy module autoload)
Ported (see Decisions log).  Deviated from the plan above: NOT a re-added
eager =(load! "lisp/sexp-transient")= lib, but a lazy general-module
autoload (modules/general/autoload/sexp-transient.el, mirrors expreg.el) -
the config's loaddefs machinery exists to keep transient prefixes lazy, and
an eager lib would pull avy + edit-indirect into startup.  The shared bypass
engine went to lisp/transient-bypass.el (required by expreg, sexp-transient
AND mpv), not back into a sexp lib - honors "don't duplicate" without the
eager load.  sp-eval-current-sexp -> eval-current-sexp (the elisp module's
user fn); dead transient--all-layout-keys dropped.  SPC k closed.
** DONE Fonts machinery (stayed direct)
Resolved with the frames lib port: config.el face bridge +
=font-size-increase/decrease/reset= over the default face height;
doom-font vars stay unvendored. Consequence: =doom-big-font-mode= will
never exist - the SPC t sitting must drop or replace SPC t b.
Amendment (2026-07): the bridge was incomplete - Doom also pins
fixed-pitch <- doom-font and fixed-pitch-serif <- doom-serif-font
(lisp/doom-emacs.el:1137), a user-theme spec that outranks themes.
Without it, ag-themes-spacemacs-light's stale =fixed-pitch :family
"JetBrains Mono"= (font not installed; ag-themes-base16-ocean carries
the same line) won in the lab and NS fell back to Skia - org
blocks/code rendered PROPORTIONAL. Bridge now pins fixed-pitch (:width
normal shadows the theme's expanded) + fixed-pitch-serif to Fira Code;
GUI probe shows resolution byte-identical to Doom. The theme-side rot
is flagged for an ag-themes upstream fix.
Amendment (2026-07): the companion fonts-relative-height-h hook (Doom PR
#8733 restoration) is commented out in config.el - a confirmed no-op here.
It only rewrites INTEGER :height on fixed-pitch/-serif/variable-pitch, but
ag-themes pins fixed-pitch :height 1.0 and config.el sets variable-pitch
1.125 (both floats); fixed-pitch-serif stays unspecified - no integer ever
reaches it. Doom needed it because doom-font baked absolute heights, which
the lab doesn't vendor. Candidate for deletion once daily theme/zoom use
confirms org/code blocks still scale with font-size-increase.
** DONE Theme: colors module
Ported (see Decisions log). circadian owns day/night loading off
doom-init-ui-hook; base16-theme was missing from this list (packages.el
was truth). SPC T closed.
** DONE Root packages leftovers
golden-ratio + lorem-ipsum landed with the elisp sitting; SPC w closed.
** DONE LSP_USE_PLISTS env
Set in early-init.el (=(setenv "LSP_USE_PLISTS" "1")=) with the lsp module
port - in the env for both the elpaca byte-compile and load.
** TODO lisp/elisp-lint.el
Flymake backend flagging let+if -> if-let* patterns; port with elisp module.
** TODO Trial gates still open
- Linux parity: automated - the CI smoke job boots on ubuntu; keep it green
- [X] one elpaca-manager update cycle (fetch, review, merge) - completed
  live 2026-07 (first lab-hosted sitting); all three failures root-caused,
  see the update-cycle Decisions entry
- [X] local-repo recipe pattern proven with the git module port (remoto +
  github-topics build in place; see "Local :repo packages")

* Custom modules
** DONE git
Ported (see Decisions log). Left behind on purpose: git-timemachine +
smerge (the :emacs vc fold-in decision, SPC g t parked), orgit/orgit-forge
(org module), consult-gh config + embark glue (disabled upstream / embark),
gist (commented out in doom.d). let-plist stays unported - git-module
consumers rewritten with plist-get; url.el keeps waiting (embark, dash,
org-cliplink, let-plist), though its top-level ghub require now resolves.
** DONE org (core slice; long tail pending)
Core slice ported - see Decisions log. SPC o r tree + SPC r r alive,
vulpea-insert restored in expreg-transient. org-superstar joined the core
slice later (daily-visible bullets; see the daily-driver fixes entry).
org-cliplink pulled ahead 2026-07 (first lab-hosted sitting): package +
"i L" localleader restored; the five url.el title-retrieval sites and
web-browsing's org branch heal via :commands autoloads
(org-cliplink-retrieve-title-synchronously carries no upstream cookie -
the use-package :commands list covers it explicitly).
Left for a long-tail sitting:
verb, anki-editor(+anki-gen), ob-* extras (ob-async/http/mermaid),
org-pomodoro, toc-org, orgit(-forge) (still back-fills the git deferral),
org-edit-indirect, ox-clip/ox-gfm,
org-download, org-contrib, the 2 mp3 assets, org-capture frame infra
(spacehammer flow), org-refile/org-avy commands, org-pretty-mode
(org-present), export-to-clipboard helpers, edit-indirect-guess-mode-fn+.
Feeds embark (vulpea-*), now unblocked.
browser-create-roam-node-for-active-tab landed with the web-browsing port.
** DONE ai
Ported (see Decisions log): gptel, eca, mcp, gptel-agent,
gptel-anthropic-oauth + parseedn; gptel-tools/gptel-anthropic-oauth
vendored under modules/ai/. Dropped: whisper, gptel-quick, ob-gptel,
llm-tool-collection, ragmacs, shell-maker, acp, agent-shell,
claude-code. =C-x g= / =SPC x g= / =SPC o c g= live.
** DONE web-browsing
Ported (see Decisions log).  elfeed stack + yeetube dropped, mpv
promoted to explicit, four local repos on the recipe +
local-dev-packages pattern (browser-hist.el + consult-hn extracted to
~/GitHub/agzam), readable/ npm workdir quarantined to the cache dir,
expreg-transient browser-url inserters restored.  Unblocks
writing/embark/search (eww-* helpers + browser-hist); their doom.d
configs still use the pre-rename +names.
CAVEAT: hnreader + reddigg use-package blocks are commented out - the
forks are currently broken (their installs also hung CI smoke);
re-enable in config.el once the packages are fixed.
** DONE writing
Ported (see Decisions log).  jinx, sdcv, mw-thesaurus, google-translate
(fork, :branch improvements, posframe UI rides the build), define-it,
separedit, youtube-sub-extractor, occult, prisma (install-only),
wiktionary-bro (extracted to ~/GitHub/agzam), spacehammer (local-dev via
~/.hammerspoon).  abbrev_defs tracked in-module and written in place
(sanctioned exception #2).  SPC x l / SPC x t trees closed; insert-dash +
insert-bracket-pair un-voided (org "[ SPC" still rides vulpea's config,
doom.d-parity).  Unblocked: clojure (spacehammer-monroe), chat (jinx).
Parked with their modules: grip block (:lang markdown), lsp-marksman +
harper activation (lsp; the after! lsp-mode harper registration ported
inert), menu-bar clock/pomodoro fn (org long-tail).
translate-at-point-smart landed with the pdf module (2026-07) - it lives in
writing/autoload/google-translate.el, nov's localleader "T" is its consumer.
** DONE embark
Ported (see Decisions log). embark + embark-consult (package-only
wiring); SPC a / [remap describe-bindings] / minibuffer export rows
bind at startup; url-action table on the lab renames with
magit-clone-simple at "g c"; embark-project-search rides
consult-ripgrep until search ports. Fixed on the way: url.el's void
bug-reference converters (+ specs), ephemeral-act's minibuffer
R-prefix pollution, link-org->just-text's missing cookie.
reddigg/hnreader/youtube-sub action rows stay runtime-void (parked
packages). C-; / SPC a / C-SPC embark-preview / vertico ";" subkeys /
avy "." / SPC b a all live.
** DONE search
Ported (see Decisions log). consult-omni UNPINNED on develop (:files
recipe, canonical sources API, keys over auth-host->pass, filter-args
no-match advice), tldr, slacko (LOCAL-REPO; slacko-search supersedes
the hammerspoon search-in-slack), consult-symbol + zoxide-find
relocated home. SPC * / SPC s tree / SPC f d live, SPC s cluster
closed. Rode along: process-external-url rewritten over
embark-url-config, the consult-customize *Warnings* pair vendored into
bindings/autoload.el, embark-project-search rebind declared final.
** DONE tab-bar
Ported (see Decisions log). Registry-only, everything in the lazy
autoload/. Built-in collisions renamed (tab-bar-rename/-duplicate,
tab-move-left/right), the dead hook-defvar pair and switch-to-tab-number
dropped, rename-dups' first-group-only bug + the desktop exit-save
prompt rot fixed. SPC l live; SPC p + SPC q closed; window-undo/redo
finally record (tab-bar-history-mode was never on before).
** DONE modeline
Ported (see Decisions log). `main' redefined once over doom.d's
per-window-change buffer-local stamping; persp-name and the dead sets
dropped.
** DONE colors
Ported (see theme infra item + Decisions log). ag-themes extracted to
~/GitHub/agzam/ag-themes.el; ef-themes left out on purpose.
** DONE dired
Ported (see Decisions log). treemacs-projectile stayed out (projectile
drop), lsp-treemacs parked on lsp, winum unignore glue dropped as
verified rot. SPC p T/t alive; SPC t d was already live and its
dired-sidebar block moved home from general.
** DONE elisp
Ported (see Decisions log). let-plist + evilify-edebug folded into module
autoload/ (no local repos - straight-era workaround retired), macrostep +
eros added explicitly, buttercup stays a bb-deps concern (nothing installs
it at runtime). Rode along: helpful/golden-ratio/lorem-ipsum/consult-symbol
in the root layer, smartparens base, the *scratch* replacement.
** DONE shell
Ported (see Decisions log). shell-pop, ghostel+evil-ghostel (:files split),
eshell-atuin, kkp, vimrc-mode; mise and yuck-mode dropped. Doom :term eshell
aliases/prompt folded in; the aliases-file relocate note resolved by dropping
the setq - the file never existed, the folded alist was the effective set.
shell-pop-in-project-root rebuilt on project.el. Closed SPC ' / SPC ".
** DONE tree-sitter
Ported (see Decisions log). treesit block gated to Emacs 31 (CI's 30.1
lacks the API), grammar ensure-installed deferred to consumer load so the
'ask policy can't prompt mid pty boot, fold-all advice extracted to module
autoload/. evil-matchit going live self-activated dired's
evil-matchit-off-h guard (probe-verified).
** DONE lsp
Ported (see Decisions log). lsp-mode/lsp-ui/consult-lsp/dap-mode + the
useful slice of Doom :tools lsp +peek; LSP_USE_PLISTS + read-process-output-max
handled; the lookup-handler registration moved out of consult-dash's
:config (load-order fix). lsp-ui-flycheck-list+ dropped as doom.d rot.
** DONE clojure
Ported (see Decisions log). cider/clj-refactor/clojars/neil/logview/
datetime/cider-storm/monroe; lookup handlers via the vendored core,
projectile -> project.el, spacehammer-monroe unblocked (darwin-only,
fennel map! waits on lua), expreg Clojure section + org-cliplink restored.
** TODO notmuch
notmuch (gh), org-mime, ol-notmuch, consult-notmuch. modulep! conditionals
inside packages need static resolution. doom-real-buffer-functions (shim).
** TODO chat
telega. jinx dep (writing). config 38L.
** DONE pdf
Ported (see Decisions log).  Continuous scroll is UPSTREAM now
(vedang/pdf-tools ships pdf-roll.el / pdf-view-roll-minor-mode as of v1.2.0,
2025-12), so the dalanicolai pdf-roll fork branch AND image-roll are dropped;
the recipe tracks vedang's default branch and keeps build/{Makefile,server}
for the runtime epdfinfo compile (first PDF open, never at elpaca build time).
Kept: saveplace-pdf-view, org-noter, nov.  Dropped: calibredb (unused, user
call).  Added: hide-mode-line (annotation-list mode-line).  Cross-module:
translate-at-point-smart un-parked into writing; org-mode-map + pdf-view-mode-
map localleader "n" and SPC n e wired to org-noter (SPC n e fills the slot
Doom's (modulep! :lang org +noter) guard prunes in the :custom-org lab).
** DONE jira
posframe + LOCAL-REPO go-jira. config 46L.
Ported (2026-07). Darwin-only, mirroring doom.d's
(:if (featurep :system 'macos) jira): the browse autoload rides the
web-browsing module's JXA browser fns (browser-get-tabs/-activate-tab).
Spliced into active-modules and doom-modules-enabled behind
(eq system-type 'darwin), after osx.  go-jira joined local-dev-packages
(~/GitHub/agzam/go-jira.el - same in-place build as remoto.el; already a
git checkout, doom.d symlinked it into the module dir).
Deltas from doom.d:
- Dropped the go-jira-browse-ticket-mode-map / -browser-ticket-mode-get-url
  block: the package rewrite removed that mode; browsing now shells out to
  the OS browser via go-jira-browse-ticket-url (view/board/edit modes stay).
- Board defaults (go-jira-default-board-id 3018, -board-name "SAC Pipeline
  (Pod 2)") moved from doom.d custom.el into config.el setopt, next to the
  SAC search string; interactive go-jira-set-default-board still wins via
  custom.el (loads last).  go-jira-default-project already defaults to SAC.
- The two module autoloads gained (require 'go-jira): they call private,
  cookie-less helpers (go-jira--find-exe, -ticket-arg-or-ticket-at-point)
  that void-errored on a cold M-x under doom.d.
- tab-bar new-tab transient's darwin-gated "jb" -> go-jira-browse-default-board
  restored (the tab-bar port dropped it pending this module).
Deps already present (markdown-mode/s/consult/posframe built transitively;
jira CLI + jq on PATH).  Tests cover both module autoloads (branch matrix +
the require fix) and the tab-bar entry.
** DONE osx
ob-applescript, applescript-mode. 15L. Trivial.
Ported (2026-07). Darwin-only: spliced into active-modules and
doom-modules-enabled behind (eq system-type 'darwin) - the Doom :if
equivalent. mac-right-option-modifier 'meta restored (doom-defaults'
'none silently downgraded right-Opt+Backspace from M-DEL to DEL).
Delta: ob-applescript merges into org-babel-load-languages instead of
clobbering the org module's list like the doom.d original did.
Also folds in the Doom os/macos subset: mdfind locate, ns-pop-up-frames,
keychain auth-sources, trash, ns-auto-titlebar, and the open-with pair
as macos-open-with/macos-open-in-default-program (the root config.el
SPC f O binding was void until then). Dropped: emacs-mac-port-only
scroll vars, iTerm/Transmit/LaunchBar open-with family.
** TODO learning
exercism. 11L. Trivial.
** DONE python
Ported (see Decisions log). python (built-in) + lsp-pyright (basedpyright +
ruff + uv, toolchain current for 2026); autoload.el 294L folded into
autoload/python.el (dash-free FQN via when-let*, plus-free renames:
python-lookup-handlers-h, python-fully-qualified-symbol-at-point). Deviations:
localleader map! + ruff python-fix-imports advice armed in the `python' block,
lsp-ruff-python-path via after! lsp-ruff.
** TODO golang
go-mode, gorepl-mode, gotest. set-lookup-handlers!/set-repl-handler!
(stubs exist / add repl stub). 30L.
** DONE lua
Ported (see Decisions log). lua-mode is BUILT-IN as of Emacs 31 (immerrr's
mode upstreamed into progmodes/; :ensure nil, MELPA package dropped);
fennel-mode + fennel-ls kept (manual client registration, imenu remap);
set-lookup-handlers! x2. friar DROPPED (AwesomeWM/Linux fennel REPL, unused
on macOS - Hammerspoon owns Lua/Fennel via spacehammer + clojure's monroe).
Activates the clojure module's inert :after fennel-mode monroe keymap.
** TODO webdev
emmet, prettier.el, xref-js2, html-to-hiccup, jtsx, js-comint. 45L.
** DONE yaml
Ported (see Decisions log). yaml-mode, jinja2-mode, highlight-indent-guides,
yaml-pro (GitHub pin). doom-theme reference guarded on custom-enabled-themes;
the two highlight-indent-guides hook fns extracted plus-free to autoload/;
yaml-pro-ts-mode rides the tree-sitter module's already-wired yaml grammar
ensure. No new stub infra needed - the reason it went ahead of golang/nix
(set-repl-handler!) and lua (friar/fennel-ls/spacehammer entanglement).
** TODO nix
nix-mode, nix-update. set-repl-handler! stub. 35L.
** TODO java
lsp-java. 8L. Trivial.
** TODO flutter
dart-mode, lsp-dart, flutter. set-ligatures! (stub or drop). 53L.
** TODO rust
rust-mode, rustic. 5L. Trivial.

* Official Doom modules: port or drop
Each needs a lab decision; bindings tree + registry already assume them.
- :emacs undo -> covered by infra undo item
- :config default +smartparens -> covered by infra item
- :tools lookup :: DONE 2026-07 - vendored as modules/lookup/ (rebuild,
  plus-free); set-lookup-handlers! + dispatch + backends + provider alist +
  K/gd bindings. Unblocks golang/lua/consult-dash bindings.
- :tools eval +overlay :: set-repl-handler!/set-eval-handler! + eros overlays.
  Needed by golang/nix REPL handlers.
- :tools lsp +peek :: DONE 2026-07 - folded into the custom lsp module
  (deferred-shutdown, signature-stop, lookup handlers, manual-only lsp-ui).
- :editor multiple-cursors :: evil-mc + evil-multiedit (evil-multiedit
  referenced by evil-escape excluded-states already).
- :editor file-templates :: yasnippet templates infra; elisp module ships own.
- :ui window-select :: ace-window (SPC w W/M/D bindings reference it).
- :ui zen :: writeroom-mode (+ config.el writeroom guards already ported).
- :ui vi-tilde-fringe :: cosmetic, trivial.
- :emacs vc :: git-timemachine, smerge hydra; fold into git module port.
- :emacs ibuffer :: groups/icons; general module has own ibuffer config -
  maybe drop Doom's.
- :emacs electric, tramp :: small defaults; likely inline into config.el.
- :term eshell :: aliases/prompt folded into the shell module port
  (2026-07); registry entry stays for the bindings-tree guard.
- :checkers syntax +childframe :: flycheck + posframe; their config disables
  global-flycheck - decide scope.
- :checkers grammar :: langtool/writegood; likely drop (writing module covers).
- :os macos :: folded into the custom osx module port (2026-07), see the
  osx entry above for the kept/dropped split.
- :lang emacs-lisp/json/markdown/sh :: port with respective language work;
  markdown +grip referenced by writing autoloads.

* Local :repo packages (14)
gptel-tools, gptel-anthropic-oauth, github-topics, remoto, ag-themes,
slacko.el, spacehammer (symlink!), wiktionary-bro,
prisma.el, browser-hist, consult-hn, emacs-reddig, navegosa.el, go-jira.
(let-plist + evilify-edebug left the list - folded into the elisp module's
autoload/ instead; the local-repo ceremony was a straight-era workaround.
gptel-tools + gptel-anthropic-oauth settled differently at the ai port:
no upstream remotes, so they vendored INTO the tree at modules/ai/ with
in-place elpaca builds - the pattern for embedded locals that have no
GitHub repo to extract to.)

Layout decided and PROVEN at the git module port (2026-07): module configs
declare plain GitHub recipes (portable to machines without checkouts);
init.el's =local-checkout-recipe= on =elpaca-recipe-functions= redirects
any package listed in =local-dev-packages= to its ~/GitHub/agzam/ checkout
when that dir exists - elpaca then builds IN PLACE (edits picked up on
=elpaca-rebuild=, nothing cloned). Extend the alist as local packages port.
Gotchas learned live:
- The trailing slash on the local path is load-bearing:
  =elpaca-git-repo-dir= strips a bare =\.el$= from the repo name, which
  demoted remoto.el to clone mode (it cloned into ~/GitHub/agzam/remoto
  before the fix). =file-name-as-directory= in the hook handles it.
- The cons form =:repo ("~/path" . "name")= CLONES into
  elpaca-sources-directory - never use it for the work-locally flow.
- The path must be a git repo root; elpaca's git ops against in-place repos
  were observed read-only (github-topics reflog stayed clean).
- Embedded doom.d submodules need extraction first: github-topics now lives
  at ~/GitHub/agzam/github-topics (cloned off the submodule preserving the
  then-unpushed dce3530, origin re-pointed to GitHub; pushed 2026-07,
  upstream tracks) and ag-themes at ~/GitHub/agzam/ag-themes.el (af440d2,
  already in sync with GitHub at extraction). browser-hist.el + consult-hn
  followed at the web-browsing port - fresh clones (the submodules held
  nothing unpushed; doom.d's browser-hist was 2 behind origin/main).
  wiktionary-bro.el followed at the writing port (fresh clone, one ahead
  of the submodule - an upstream PR merged since; nothing unpushed).
  Remaining doom.d-embedded locals (let-plist, evilify-edebug, gptel-*,
  ...) get the same treatment when their modules port; the doom.d
  submodules dissolve when the cord is cut.
- A checkout need not live under ~/GitHub/agzam: spacehammer's local-dev
  entry points at ~/.hammerspoon - the hammerspoon config IS the package
  checkout (doom.d symlinked spacehammer.el out of it instead).
  google-translate/occult/prisma checkouts already existed there, all
  clean and pushed at the writing port (google-translate's improvements
  branch carries google-translate-posframe.el; master doesn't).
  go-jira landed the same way at the jira port (2026-07): the checkout
  already sat at ~/GitHub/agzam/go-jira.el (doom.d symlinked it into the
  module dir), clean on main - just added to local-dev-packages.

Settled: general's bare let-plist uses (autoload/url.el x4, autoload/misc.el)
resolve at call time via the elisp module's loaddefs macro-autoload. The git
module had rewritten its own uses with plist-get before that existed.

* Assets to relocate
- snippets: currently read from ~/.doom.d/snippets (62 snippets); localize
  to lab when cutting the cord
- org module mp3s; elisp templates/, web-browsing readable/ and writing
  abbrev_defs relocated with their modules (2026-07; readable's npm
  install/node_modules quarantined to the cache dir, abbrev_defs tracked
  AND written in place - sanctioned exception #2 in AGENTS.md; writing's
  number-to-words npm workdir quarantined to the cache dir too)

* Cross-module port ordering constraints
- web-browsing before: search - satisfied, search landed 2026-07
- writing landed (2026-07): clojure (spacehammer-monroe) and chat (jinx)
  unblocked
- org before: embark (vulpea actions)
- lookup decision before: golang, lua, clojure, consult-dash bindings -
  satisfied, lookup core landed 2026-07 (clojure consumed it)
- smartparens before: sexp-transient, general sp-pairs activate -
  satisfied, sexp-transient restored 2026-07 (lazy autoload; avy +
  edit-indirect stay out of startup)
- lsp before clojure: satisfied - ported together 2026-07 (entangled;
  clojure autoloads call lsp-lookup-*, lsp-clojure-*, lsp-completion-at-
  point-maybe)
- lsp + clojure before python + lua: satisfied - both landed 2026-07 after
  clojure; python/lua call lsp! and the lsp lookup handlers, and lua's
  fennel-mode arms the clojure module's inert :after fennel-mode monroe
  keymap ("fennel map! waits on lua")
