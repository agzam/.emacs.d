# All machine state - elpaca repos/builds, caches, eln, grammars, package
# droppings (history, recentf, transient/, url/, ...) - lives in .local/.
/.local/

# custom-file lives here by choice (edited in place) but is never tracked.
/custom.el

# Per-tool droppings at the repo root.
/.claude/
/.clj-kondo/
/.lsp/

# Build artifacts and editor litter, anywhere.
*.elc
*.eln
*~
\#*\#
.\#*
.DS_Store
